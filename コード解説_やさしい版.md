# LLM Local Chat ― コード解説（やさしい版）

作成日: 2026-08-17
対象読者: 技術職から離れて久しい方（プログラミングの基礎知識はあるが、最近のAI周辺の用語は追っていない方）
対象コード: `D:\AI\LLM\LLM_Local_Chat` および全サブディレクトリ
書き方の方針: **実際にコードを読んで確認できた事実だけ**を書く。推測は「※推測」と明記する。

> **⚠️ この文書が説明している範囲について**
>
> この文書は上記の**作者のローカル環境にあるフォルダの棚卸し**です。**GitHubで公開しているリポジトリには、ここに挙げたファイルの一部しか含まれていません。**
>
> 特に次のものは**リポジトリに含まれていません**。クローンしても入手できないため、各自で用意する必要があります。
>
> - **GGUFモデル本体**（`.gitignore` が `models/` と `*.gguf` を除外。リポジトリには 0バイトの案内ファイルが1個あるだけ）
> - **Whisperモデル**（初回起動時に自動ダウンロード）
> - **`.venv/`**（Python実行環境。各自で作成する）
> - **`chat_logs/`**（作者の会話履歴。個人データ）
> - **`OLD_DATA/`、`docs/` 配下の一部、作者のローカル運用メモ**
>
> どのファイルが公開対象かは [5-3節](#5-3-コード以外のファイル) の「公開」列にまとめました。導入手順は `README.md` を参照してください。

---

## 目次

1. [30秒でわかる「これは何か」](#1-30秒でわかるこれは何か)
2. [先に押さえておきたい7つの言葉](#2-先に押さえておきたい7つの言葉)
3. [全体像 ― 小さな会社にたとえる](#3-全体像--小さな会社にたとえる)
4. [起動してから返事が返るまで（時系列）](#4-起動してから返事が返るまで時系列)
5. [ファイル別ガイド](#5-ファイル別ガイド)
6. [技術的に面白い10の仕掛け](#6-技術的に面白い10の仕掛け)
7. [データはどこに置かれるか](#7-データはどこに置かれるか)
8. [設定ファイルの中身](#8-設定ファイルの中身)
9. [テストについて](#9-テストについて)
10. [開発の歴史（バージョン履歴の要約）](#10-開発の歴史バージョン履歴の要約)
11. [読んでいて気づいた点](#11-読んでいて気づいた点)
12. [用語集](#12-用語集)

---

## 1. 30秒でわかる「これは何か」

**自分のパソコンの中だけで動く、日本語のAIチャットアプリ**です。Windows専用。

- ChatGPTのようなクラウドサービスには**一切つながっていません**。AIの頭脳そのもの（約2〜5GBのファイル）がPCの中にあり、そこで考えています。
- キーボードで打つだけでなく、**マイクに話しかける**こともできます。
- AIの返事を**声で読み上げ**ます。画面の隅に立つキャラクター（「シロ」）が、それに合わせて瞬きと口パクをします。
- 「体重62kgだった」と話すと**健康記録データベース**に、「コンビニで500円使った」と話すと**家計簿データベース**に登録できます（毎回、確認画面が出ます）。
- 会話の記録は**暗号化して**自分のPCに保存されます。

規模感としては、Python のコードが約 **10,300行**（本体2,850行＋モジュール群）、それとは別にテストコードが約 **4,600行**あります。個人開発のアプリとしてはかなり作り込まれた部類です。

---

## 2. 先に押さえておきたい7つの言葉

このアプリを理解するのに必要な、比較的新しい用語だけを7つ挙げます。

### ① LLM（大規模言語モデル）
「次に来そうな単語」を延々と予測することで文章を作る、巨大な計算モデルのこと。ChatGPTの中身もこれです。このアプリは**そのファイルをPCの中に置いて**使います。

### ② GGUF ファイル / llama.cpp
LLMの中身を1つのファイルにまとめた形式が **GGUF**。`models\` フォルダに置く `.gguf` ファイルがそれです。

**このモデル本体はリポジトリに含まれていません。** Hugging Face などから各自でダウンロードして `models\` に置く必要があります（手順は `README.md` の「GGUFモデルを用意する」）。リポジトリの `models\` には 0バイトの案内ファイルが1個入っているだけです。

参考として、作者の開発機に置かれている3個は次のとおりです。**これらが配布物に含まれるわけではありません。**

```
models\gemma-4-E4B-Q4_K_M.gguf        約 5.3 GB
models\gemma-4-E4B-it-Q4_0.gguf       約 4.6 GB
models\granite-4.1-3b-Q4_K_M.gguf     約 2.1 GB
```

**llama.cpp** は、このファイルを読み込んで動かすエンジン。Python から呼ぶための包み紙が `llama-cpp-python` というライブラリです。

> たとえ: GGUFが「楽譜」、llama.cppが「演奏する楽器」、このアプリが「楽器を操作するリモコンと客席」。

### ③ トークン と コンテキスト長
LLMは文章を「トークン」という細かい単位（日本語なら1〜2文字くらい）に分けて扱います。そして**一度に読み込める量に上限**があります。それが「コンテキスト長」（設定名 `n_ctx`、既定 8192トークン）。

これがこのアプリの設計に大きく影響します。会話が長くなると全部は入らないので、**どこを捨てるか**を決める仕組みが必要になります（→ 6章②）。

### ④ VRAM（ビデオメモリ）
グラフィックボード（GPU）が持つ専用メモリ。LLMを高速に動かすにはここに載せる必要がありますが、**足りないとアプリごと落ちます**。このアプリはVRAM切れ対策にかなりの分量を割いています（→ 6章④）。

### ⑤ Whisper
OpenAIが公開している、音声を文字にするモデル。無料でPCにダウンロードして使えます。マイク入力の文字起こしに使っています。

### ⑥ SAPI5
Windowsに昔から入っている読み上げ機能（Speech API）。追加インストール不要で日本語を喋らせられます。TTS（Text To Speech＝音声合成）に使っています。

### ⑦ スレッド
1つのプログラムの中で、複数の作業を同時進行させる仕組み。このアプリは**6種類のスレッド**が同時に動いています。

| スレッド | 仕事 |
|---|---|
| メイン | 画面の描画・クリック処理（Tkinter の `mainloop`） |
| LLM | AIが考える（重い。数秒〜数十秒かかる） |
| TTS再生 | 声を出す |
| 音声認識 | マイクを常時聞いている |
| リソース監視 | 0.5秒ごとにVRAM残量を測る |
| 連携API送信 | 家計簿・健康記録の送信 |

**なぜ分けるのか**: 全部を1本でやると、AIが考えている10秒間、画面が完全に固まって「応答なし」になってしまうからです。

---

## 3. 全体像 ― 小さな会社にたとえる

このアプリは1つの巨大なファイルではなく、**役割ごとに部署が分かれた会社**のような作りになっています。

```
                     ┌──────────────────────┐
                     │  LLM_Local_Chat.py   │  ← 社長室＋受付（画面すべて）
                     │  ChatApp クラス      │
                     └──────────┬───────────┘
                                │ 指示
                     ┌──────────▼───────────┐
                     │    controller.py     │  ← 部長（判断だけする。実作業はしない）
                     └──────────┬───────────┘
        ┌───────────┬───────────┼───────────┬──────────────┐
        ▼           ▼           ▼           ▼              ▼
 ┌────────────┐┌──────────┐┌──────────┐┌────────────┐┌──────────────┐
 │llm_service ││prompt_   ││resource_ ││json_       ││integrations  │
 │            ││builder   ││manager   ││extractors  ││              │
 │AIを動かす  ││質問文を  ││生成量を  ││返事から    ││外部へ送信    │
 │だけ        ││組み立て  ││決める    ││JSONを拾う  ││（要確認）    │
 └────────────┘└──────────┘└──────────┘└────────────┘└──────────────┘

    別動隊（それぞれ独立して動く）:
 ┌──────────────┐ ┌───────────────────┐ ┌──────────────┐
 │audio_workers │ │ resource_monitor  │ │session_store │
 │声を聞く／喋る│ │ VRAM見張り        │ │履歴を保存    │
 └──────────────┘ └───────────────────┘ └──────────────┘
```

この分け方には、はっきりした設計思想があります。`controller.py` の冒頭コメントにこう書かれています。

> Controller: 薄いオーケストレーター。**判断のみ行い、実行は各サービスに委譲する。**

つまり「部長は判断だけ、手は動かさない」。この方針のおかげで、**AIを動かす部分だけをテストする**といったことが可能になっています（実際、テストコードの多くがこの構造に依存しています）。

---

## 4. 起動してから返事が返るまで（時系列）

### 【起動編】バッチファイルをダブルクリックしてから

普段使うのは `Start_Shiro.bat` です。中身を追うと4段階になっています。

```
[1/4] 家計簿のDocker（MySQL + API）を起動      C:\Users\shino\python-mysql-dev
[2/4] 家計簿ブリッジを起動（ポート 8767）      D:\AI\kakeibo
[3/4] Biolog（健康記録）のAPIとUIを起動        D:\AI\biolog
[4/4] .venv を有効化して LLM_Local_Chat.py を起動
```

※ `LLMローカル対話型AI.bat` という、Docker起動なしでアプリだけ立ち上げる簡易版もあります。

アプリ本体（`main()` 関数）が始まってからの順番:

| # | やること | どこ |
|---|---|---|
| 1 | 空のウィンドウを作る | `tk.Tk()` |
| 2 | 部品を3つ作る（VRAM監視、Whisper置き場、履歴保管庫） | `app_composition.create_app_deps` |
| 3 | 設定ファイルを読む → アバター窓 → 読み上げ係 → 画面を組み立てる | `ChatApp.__init__` |
| 4 | **裏方スレッドで**AIモデル（数GB）を読み込む | `_reload_llm` |
| 5 | AIの読込が終わってから、必要ならWhisperを読み込む | `_load_whisper_async` |
| 6 | 画面を操作可能にして待機 | `root.mainloop()` |

**ここがうまい点**: 手順4でモデル読込をバックグラウンドに回しているので、数GBの読み込み中でも**画面はすぐ表示されて操作できます**。読み込みが終わるまでは送信ボタンを押すと「準備中です」と出るだけ。

**もう一つ**: 手順5で「LLMとWhisperを同時に読み込まない」ようにしています。両方いっぺんにVRAMを取り合うと落ちるためです。しかもWhisperを読むのは設定で「起動時にマイクを有効にする」がONのときだけ。OFFならWhisperのぶんのメモリを丸ごと節約します。

### 【送信編】メッセージを送ってから返事が返るまで

`controller.py` の `handle_text()`（253行目〜）が起点です。

```
 1. 二重送信チェック（もう動いていたら何もしない）
 2. モデルが読込済みか確認 → まだなら「準備中」ダイアログ
 3. 入力欄を空にして、画面に「👤 あなた」＋本文を書く
 4. いまのモード（通常／家計簿／健康記録）を1回だけ決定
 5. モードに応じて「AIへの指示文」を組み立てる  ← prompt_builder
 6. 会話履歴のうち、入る分だけを選んで足す        ← トークン予算計算
 7. VRAM残量を見て、今回の生成量の上限を決める    ← resource_manager
      └─ 残量が危険水準なら、ここで実行を中止して警告
 8. 別スレッドでAIに投げる                        ← llm_service
 9. 文字が1つ生成されるたびに画面へ流し込む       ← ストリーミング
10. 句点「。」が来るたびに、その1文を読み上げに回す
11. 生成完了 → 履歴に追加 → タイトル自動生成 → 保存
12. 4ターンごとに、裏で会話の要約を作り直す
```

**注目**: 手順10。返事が全部できあがるのを待たずに、**1文できるたびに喋り始めます**。これで体感の待ち時間がかなり短くなります。

---

## 5. ファイル別ガイド

### 5-1. 中心のファイル

#### `LLM_Local_Chat.py` — 2,850行

**このアプリの顔。画面まわりのすべて。**

大きすぎるように見えますが、中身は明確に分かれています。

| 中の部品 | 行あたり | 何をするか |
|---|---|---|
| 定数・設定の読み書き | 137〜290 | 色、フォント、既定値、`chat_settings.json` の読み書き |
| `init_llm` | 292 | **AIモデルの読み込み**（後述、ここが力作） |
| `PortableExportDialog` ほか3つ | 429〜710 | 暗号化エクスポート／インポートの画面 |
| `SettingsDialog` | 712 | 設定画面 |
| `KakeiboConfirmDialog` | 916 | 家計簿登録の確認画面（金額・カテゴリを手直しできる） |
| `AvatarWindow` | 1096 | **アバター窓**（枠なし・最前面・ドラッグで移動） |
| `ChatApp` | 1237〜2810 | **メイン画面のすべて**（約1,570行） |
| `main()` | 2811 | 起動 |

**`init_llm`（292行）が面白い理由**: モデルの読み込みは失敗しやすい処理です。そこで**3段階×2系統、最大6回の再挑戦**をします。

```
GPU で試す ─┬─ ① 全速力設定（flash_attn など全部盛り）
            ├─ ② flash_attn だけ切って再挑戦
            └─ ③ 最低限の設定だけで再挑戦
   ↓ それでもダメなら
CPU で試す ─┬─ ① 全速力設定
            ├─ ② flash_attn だけ切って
            └─ ③ 最低限の設定だけで
```

しかもCPUに切り替える前に、GPUメモリの掃除（`gc.collect()` と `torch.cuda.empty_cache()`）まで挟んでいます。**「とにかく起動だけはさせる」**という執念が見えるコードです。

**`AvatarWindow` の細かさ**: 瞬きの間隔を2〜6秒のランダムにし（`BLINK_INTERVAL_MIN/MAX`）、瞬きは150ms、口パクは140ms間隔で切り替え。しかも**画像ファイルが無くても透明画像で代替して落ちない**ようになっています。

---

#### `controller.py` — 716行

**判断役。「何をするか」を決めて、「どうやるか」は他に任せる。**

3つの独立した小部品が入っています。

**① `StreamingTTSBuffer`（32行）** ― 流れてくる文字を1文ずつに切り分ける係。

面倒なのは**コードブロック**の扱いです。AIが ` ```python ... ``` ` のようなプログラムコードを返したとき、それを読み上げると意味不明な呪文になります。そこでバッククォート（`` ` ``）の数を数えて、3つ揃ったらコードブロックの内外を切り替え、中身は読み上げから除外します。

**② `LeadingJsonFilter`（79行）** ― 健康記録モードのときだけ働くフィルタ。

健康記録モードでは、AIに「まずJSONを出して、その後に自然な返事をして」と指示します。でも画面にJSONの生データが出るのは邪魔です。そこでこのフィルタが**先頭のJSONだけを隠し、その後の日本語だけを画面に流します**。しかも**文字が1つずつ届く途中でも**判定できるように、状態機械（detecting → after_json → passthrough）として実装されています。

**③ `TokenCostCache`（148行）** ― 「この会話ペアは何トークンか」を覚えておく辞書。上限2,048件のLRU（古いものから捨てる）。トークン数の計算は毎回やると遅いので、キャッシュしています。

---

#### `llm_service.py` — 195行

**AIを動かすだけの層。判断は一切しない。**

肝は `_state_lock` という鍵です。

```python
def _start(self, worker, *, reset_abort=False):
    with self._state_lock:
        if self._running:
            return False        # ← すでに動いていたら断る
        self._running = True
        self._thread = threading.Thread(target=worker, daemon=True)
        self._thread.start()
    return True
```

**なぜ必要か**: LLMは同時に2つ動かすとメモリ的にも計算的にも破綻します。ところがこのアプリには「通常の返事」「要約作成」「健康データの再抽出」の3種類の呼び出しがあり、放っておくと衝突します。この鍵で**必ず1つずつ順番に**実行されるよう保証しています。

`generate()` の末尾では性能ログを出します。

```
[Perf] prompt_build_ms=45.2 first_token_ms=380.1 output_tokens=214 tokens_per_sec=18.34 total_ms=12005.3 status=completed
```

`tokens_per_sec` の計算に一工夫あり。届いたチャンクの個数ではなく、**完成した文章をモデル自身の分割器でもう一度数え直して**います（`count_generated_tokens`）。チャンク数だと実際のトークン数とずれるためです。

---

#### `prompt_builder.py` — 163行

**AIに渡す「質問文の完成品」を組み立てる係。**

AIには毎回、こういう塊を渡しています。

```
[system] あなたは「シロ」という名前の、親しみやすく丁寧な日本語を話すアシスタントです。…
         + [モード: 健康管理]              ← モード中だけ
         + [要約]: これまでの会話の50字要約  ← あれば
[user]      3ターン前のあなたの発言
[assistant] 3ターン前のAIの返事
[user]      2ターン前の…
     :
[user]      今回の入力
```

家計簿モードと健康記録モードでは、ここに**JSONのひな形付きの指示文**が追加されます。健康記録の指示文（114行〜）は特に長く、「測定値の文をactivity_logに複製しないでください」「memoは『メモ』と明示されたものだけ」といった、**実際に起きた誤動作を1つずつ潰した跡**が読み取れます。

---

#### `session_store.py` — 520行

**会話の保存庫。**

やっていることは4つ。

1. **保存**（`save`）: 暗号化して `chat_logs\chat_20260817_143022_123456.json` のような名前で保存
2. **一覧**（`list_sessions`）: 左サイドバーに出す一覧を作る
3. **移行**（`migrate_legacy`）: 昔の平文JSONを暗号化形式に変換する
4. **期限切れ削除**（`prune_expired`）: 保存期間を過ぎたものを消す

**保存時の慎重さ**（`_write_encrypted`、86行）が印象的です。

```
暗号化する → すぐその場で復号してみる → 元と1バイトでも違えば保存を中止
```

「暗号化したはいいが復号できないファイルができた」という最悪の事態を防いでいます。

**一覧の高速化**: 会話が180件もあると、毎回全部を復号していたら一覧表示が遅くなります。そこで `_index` というキャッシュを持ち、変化のないファイルは**復号を省略**します。

判定に使う「署名」は4点セットです（`_file_signature`、44行）。

```python
return (
    stat_result.st_mtime_ns,   # 更新時刻（ナノ秒）
    stat_result.st_size,       # サイズ
    stat_result.st_ctime_ns,   # 作成時刻（ナノ秒）
    digest.digest(),           # ← 内容の SHA-256
)
```

**誤解しやすい点**: 「更新日時が同じならファイルを読まない」わけではありません。**署名を作る時点で毎回ファイル全体を読み、SHA-256 を計算しています**（1MBずつ読む）。省略できるのはその後の **DPAPI復号 + JSONパース** の部分だけです。

```python
signature = self._file_signature(path, stat)   # ← 毎回、全内容を読んでハッシュ計算
cached = self._index.get(path)
if cached and cached["signature"] == signature:
    continue                                    # ← 復号とパースだけをスキップ
```

一見無駄に見えますが、DPAPI復号はSHA-256より重いため成立する最適化です。そして**日時とサイズだけに頼らない理由**が重要です。`atomic_io` による置き換えは非常に高速なので、**サイズも更新日時も見かけ上変わらないまま中身が差し替わる**ことが起こり得ます。日時とサイズだけを見ていると、古い一覧を表示し続けてしまいます。内容ハッシュを署名に含めることで、これを確実に検出しています（CHANGELOG の v1.4.3 に「暗号化履歴が同一サイズ・同一タイムスタンプで原子的置換された場合も、内容SHA-256で一覧キャッシュの変更を検出」と記載）。

---

#### `integrations.py` — 476行

**外部（家計簿・健康記録のAPI）へデータを送る係。ここが一番、防御が厚い。**

詳しくは6章⑩で扱いますが、送信先の判定関数だけ見ておきます。

```python
def is_allowed_local_api_url(url: str) -> bool:
    return (
        parsed.scheme == "http"            # httpsは逆に禁止（外部通信の疑いがあるため）
        and parsed.username is None        # URLにユーザー名が埋め込まれていない
        and parsed.password is None        # パスワードも
        and host in {"localhost","127.0.0.1","::1"}   # 自分自身だけ
        and port in {8767, 8766}           # しかも決まったポートだけ
    )
```

条件が5つ。1つでも欠けたら送りません。

---

#### `audio_workers.py` — 746行

**耳（マイク）と口（読み上げ）。**

**`TTSWorker`（口）**: 読み上げたい文をキューに積み、専用スレッドが1つずつSAPI5に渡します。停止は `Speak("", 3)` という特殊な呼び方（フラグ3＝purge）で、再生中の音を即座に打ち切ります。

**`VoiceRecognizer`（耳）**: マイクを16kHz・モノラルで**常時**読み続けます。処理の流れ:

```
マイク読み取り（1024サンプルずつ）
   ↓
RMS（音量）を計算  → 閾値150を超えたか？
   ↓  6回連続で超えたら「喋り始めた」と判定
録音開始（0.75秒前からさかのぼって記録） ← プリロール
   ↓
30回連続で静かになる、または6秒経ったら録音終了
   ↓
Whisper で文字起こし（日本語固定、beam_size=1で速度優先）
   ↓
品質ゲート（後述）を通過したものだけ、チャットへ自動送信
```

**プリロール（0.75秒）が地味に重要**: 「喋り始めた」と気づいたときには、もう最初の一音が過ぎています。そこで常に直前0.75秒ぶんを覚えておいて、録音の頭にくっつけます。これがないと「こんにちは」が「んにちは」になります。

---

### 5-2. 小さいが重要なファイル

| ファイル | 行数 | 役割 |
|---|---|---|
| `app_composition.py` | 27 | 起動時に必要な3部品をまとめて作る「組み立て工場」。ここで作った物を他に配る |
| `atomic_io.py` | 37 | **書き込み中に落ちてもファイルを壊さない**書き込み（後述 6章⑧） |
| `history_crypto.py` | 114 | Windows DPAPI による履歴の暗号化・復号 |
| `portable_history.py` | 296 | パスフレーズ付きの持ち出し用アーカイブ（別PCへ移せる形式） |
| `json_extractors.py` | 101 | AIの返事から JSON を取り出す。`NaN`/`Infinity` のような非標準の値は**拒否** |
| `kakeibo_amount.py` | 127 | 「1万5千円」「¥1,500」などを数値に直す |
| `kakeibo_date.py` | 37 | 「おととい」「先週の金曜」などを日付に直す |
| `kakeibo_confirmation.py` | 141 | 家計簿の候補データが送信可能かを検証する |
| `resource_manager.py` | 16 | `resource_monitor` を呼ぶだけの薄い包み紙（テストしやすくするため） |
| `resource_monitor.py` | 512 | VRAM監視の本体（後述 6章④） |
| `check_audio_devices.py` | 63 | オーディオ機器の一覧を出す単独ツール。アプリ本体からは呼ばれない |

---

### 5-3. コード以外のファイル

「公開」列は、GitHubのリポジトリに含まれるかどうかを示します（2026-08-17 時点、追跡ファイル58件を基準に確認）。

| 場所（作者のローカル環境） | 中身 | 公開 |
|---|---|---|
| `README.md`（41KB） | 導入手順、動作環境、FAQ、運用者向けのセキュリティ説明 | ✅ |
| `CHANGELOG.md`（31KB） | v1.0 から v1.6.0 までの全変更履歴 | ✅ |
| `SECURITY.md` | 脆弱性報告の窓口 | ✅ |
| `LICENSE` | MITライセンス | ✅ |
| `requirements.txt` / `requirements-cpu.txt` | 依存ライブラリ（CUDA版／CPU専用版） | ✅ |
| `chat_settings.json.example` | 設定ファイルのテンプレート | ✅ |
| `avatars/` | アバター画像4枚（通常／喋り／瞬き／瞬き＋喋り） | ✅ |
| `tests/` | 回帰テスト20ファイル | ✅ |
| `docs/CURRENT_SPEC.md`（32KB） | **現時点の実装事実だけ**を書いた仕様書。「推測」と「確認済み」を明確に分けている | ⚠️ **ルート直下**の `CURRENT_SPEC.md` として公開。公開側は 2026-07-27 版で内容が古い |
| `docs/architecture_snapshot.md` | 構造のスナップショット | ⚠️ **ルート直下**の `architecture_snapshot.md` として公開 |
| `docs/architecture_slides.html` | 構成説明のスライド（HTML） | ⚠️ **ルート直下**の `architecture_slides.html` として公開。公開側は 2026-06-02 版 |
| `docs/incidents/` | 過去のトラブル記録2件 | ❌ 非公開 |
| `docs/plan_LLM_ver2.0.md` | v2.0構想 | ❌ 非公開 |
| `docs/LLMver*_prompt.txt` | 旧版の作成時プロンプト4件 | ❌ 非公開 |
| `models/` | 作者の開発機には GGUF 3個（合計 約12GB）があるが、**リポジトリには 0バイトの案内ファイル `ここにモデルを追加してください.txt` が1個あるだけ** | ❌ **モデル本体は非公開** |
| `chat_logs/` | 暗号化された会話履歴 180件（合計 約3.9MB） | ❌ 非公開（個人データ） |
| `OLD_DATA/` | 過去バージョンのソース17個（ver1.0.3〜1.1.2）。**現行コードからは使われていない** | ❌ 非公開 |
| `.venv/` | Python 3.12.10 の専用実行環境 | ❌ 非公開（各自で作成する） |
| `CLAUDE.md` / `skills.md` / `Start_Shiro.bat` / `start.sh` | 作者のローカル運用メモと起動スクリプト（ローカル絶対パスを含む） | ❌ 非公開 |
| `chat_settings.json` | 実行時設定（モデルパス、マイク・TTSの状態など） | ❌ 非公開（`.example` のみ公開） |

**`docs/` の扱いに注意**: 公開側の `.gitignore` は `docs/` をディレクトリ丸ごと除外しています。そのためリポジトリでは、公開対象のドキュメントが**ルート直下**に置かれています。この文書自身も、公開リポジトリではルート直下の `コード解説_やさしい版.md` です。

---

## 6. 技術的に面白い10の仕掛け

### ① ストリーミング表示＋文単位の読み上げ

**問題**: AIの返事は数十秒かかることがある。その間まったく何も出ないと、固まったように見える。

**解決**: `stream=True` で1文字ずつ受け取り、届くたびに画面に足していく。さらに `StreamingTTSBuffer` が「。！？改行」を検出したら、**その1文だけを先に読み上げに回す**。

```
[生成中] こんにちは。   ← ここで「こんにちは。」を喋り始める
[生成中] こんにちは。今日は  ← 喋りながら次を生成
[生成中] こんにちは。今日はいい天気ですね。 ← 「今日はいい天気ですね。」を追加で喋る
```

**注意点も設計されている**: `_on_stream_token` は毎回「操作世代（generation）」を確認します。停止ボタンを押した後に遅れて届いた文字は、世代番号が違うので**無視されます**。

---

### ② トークン予算 ― 会話履歴の取捨選択

コンテキスト長が8192トークンでも、そのすべてを会話履歴に使えるわけではありません。`prompt_builder.py` の計算式:

```
使える履歴の量 = (n_ctx − max_tokens − systemの長さ − 今回の入力の長さ) × 0.60
                    8192      1024        実測+256バッファ    実測
```

最後の `× 0.60` は安全マージン。計算どおりぴったり使うと、トークン数の数え方の微妙な差でオーバーする恐れがあるためです。

そして**新しい方から順に**、予算に収まるだけ会話ペアを詰めていきます。

```python
for h in reversed(history):        # 新しい順
    cost = キャッシュ or 計算(user + assistant + 12)
    if budget - cost < 0:
        break                       # 入らなくなったら打ち切り
    selected_history.insert(0, h)   # 先頭に挿す（時系列を保つ）
    budget -= cost
```

`+ 12` は、メッセージの区切り記号などのオーバーヘッド見積もりです。

---

### ③ 要約メモリ ― 古い会話を50字に圧縮

②で古い会話は捨てられます。ではAIは昔の話を完全に忘れるのか。そこで「要約メモリ」です。

- **4ターンごと**（`SUMMARY_THRESHOLD = 4`）に、AIに「これまでの会話を50字以内で要約して」と頼む
- できた要約を、以後のプロンプトの `[要約]:` 欄に毎回入れる
- 画面上部に「📝 メモリ:」として表示される
- セッションのJSONにも保存される

要約時のパラメータは通常生成と違います: `max_tokens=80, temperature=0.3, stop=["\n"]`。低い temperature は「創作しないで正確に」の意味。`stop=["\n"]` で1行だけに強制。

---

### ④ VRAM安全装置 ― 「管理する」のではなく「逃げる」

`resource_monitor.py` の冒頭に、この設計の哲学がはっきり書いてあります。

> 設計思想: 「VRAMを管理する」ではなく **「VRAMから逃げる」**
> - 予約・帳簿・状態管理は持たない。即時の観測値だけで判断する。
> - OOM完全防止は不可能。**「クラッシュではなく劣化」で済ませることが目標**。

正直な但し書きも添えられています。

> 既知の限界:
> - pynvml は allocator 後スナップショット（TOCTOU あり）
> - 単一プロセス内のみ有効（他プロセスの VRAM 確保は観測不可）
> - この設計は確率的削減が目的であり、OOM の完全防止は保証しない

守りは3層です。

**第1層: モデル読込の前**（`adjust_llm`）

```
必要量 = GGUFファイルのサイズ + 起動予約（1024MB か 総容量の12% の大きい方）
空きが足りる  → GPUに全部載せる（n_gpu_layers = -1）
足りない      → GPUを使わずCPUで動かす（n_gpu_layers = 0）
```

**第2層: 推論の直前**（`adjust_inference`）― 毎回チェックする

```
最低確保量 = 512MB か 総容量の6% の大きい方
空き < 最低確保量        → 実行を中止（「VRAM使用率が高いため実行できません」）
空き < 1024MB            → 生成上限を 1/4 に（最低256は残す）
空き < 1536MB            → 生成上限を 1/2 に
それ以上                 → そのまま
```

さらに、Whisperが直前にGPU使用率を10%以上跳ね上げていたら、**もう一度半分**にします。

**第3層: Whisper のGPU/CPU自動切替**（`WhisperController`）

```
GPU使用率が 88% を超えた → CPU版に切り替え
GPU使用率が 70% を下回った → GPU版に戻す
```

`88` と `70` の間に差を設けている（ヒステリシス）のは、境界付近でGPU↔CPUをパタパタ往復しないようにするためです。

Whisperのモデルサイズも空き容量で自動選択されます。

```
空き 4096MB以上 → medium（高精度）
空き 2048MB以上 → small
それ未満        → CPU版 small
```

**測定手段も三段構え**: `pynvml`（NVIDIAの公式ライブラリ）→ ダメなら `nvidia-smi` コマンド → それもダメならCPU専用モードで動作。

---

### ⑤ 音声認識の「幻聴」対策

Whisperには有名な弱点があります。**無音や雑音を聞かせると、勝手に文章をでっち上げる**のです。定番の幻聴が「ご視聴ありがとうございました」「チャンネル登録をお願いします」。YouTube の字幕データで学習した名残です。

`audio_workers.py` はこれに5段構えで対処しています。

**1. 決め打ちの除外リスト**

```python
WHISPER_NOISE = {"字幕", "翻訳", "。", "、", "…", " ", "　"}   # 完全一致で捨てる

WHISPER_NOISE_PARTIAL = {   # 含まれていたら捨てる
    "ご視聴ありがとうございました",
    "チャンネル登録をお願い",
    "高評価よろしくお願い",
    "概要欄をご確認",
    ...
}
```

ファイル冒頭のコメントに開発の経緯が残っています。

> WHISPER_NOISE から「ありがとうございました」「おやすみなさい」を除去
> → 実発話が誤ってノイズ判定されていた

つまり最初は乱暴に切りすぎて、**本物の「ありがとうございました」まで消えてしまった**。そこで部分一致リストを別に作り、「ご視聴」が付くものだけを狙い撃ちするようにした、という改善です。

**2. 統計値による判定**（`classify_recognition_quality`）

Whisperは認識結果と一緒に、区間ごとの指標を返します。

| 指標 | 意味 | 使い方 |
|---|---|---|
| `no_speech_prob` | 「これ無音だと思う」度合い | 高すぎたら捨てる |
| `avg_logprob` | 自信の度合い（負の数） | 低すぎたら捨てる |
| `compression_ratio` | 同じ言葉の繰り返し度合い | 高い＋無音疑いなら捨てる |

閾値はすべてファイル上部の1ブロックに集約され、各行にコメント付きで根拠が書かれています。

```python
WHISPER_COMPRESSION_LIMIT = 2.40             # compression異常の基準(単独では破棄しない)
WHISPER_COMPRESSION_NO_SPEECH_LIMIT = 0.50   # 高compression時に無音疑いとする実測由来の下限
WHISPER_PROTECT_NO_SPEECH_MAX = 0.30         # 実発話保護: no_speech平均がこれ以下
```

**「単独では破棄しない」という但し書きが重要**です。「同じ言葉の繰り返しが多い」だけでは捨てません。本当に繰り返して喋る人がいるからです。**繰り返しが多い かつ 無音の疑いも強い** ときだけ捨てます。

**3. 長さ**: 100文字を超えたら捨てる（一度の発話でそこまで長くならない）
**4. 重複**: 10秒以内に同じ文字列が来たら捨てる
**5. TTS中**: 読み上げ中はマイク入力を丸ごと読み捨てる（→⑥）

---

### ⑥ ハウリング防止と「世代番号」

**問題**: スピーカーからAIの声が出ている間、マイクがそれを拾って「ユーザーが喋った」と誤認する。AIが自分の声に返事をして無限ループになる。

**当初の対策**: TTS中はVAD閾値を10倍に上げる → **失敗**。冒頭コメントに実測値が残っています。

> 開放型イヤホン（Float Run）環境でTTS音声RMS=2530が閾値1500を超えてしまい
> TTS読み上げ音声をユーザー発話として誤認識していた

**現在の対策**: 閾値をいじるのをやめ、**TTS中はマイクのデータを完全に読み捨てる**。

ただしこれには別の問題があります。読み上げが終わったら耳を戻すわけですが、Windowsの音声出力には残響のようなタイムラグがあるため、**800ms待ってから**戻します。ところがその800msの間に**次の読み上げが始まる**かもしれない。その場合、古い復帰処理が動くと、新しい読み上げの最中にマイクが開いてしまいます。

**解決**: TTSに「世代番号」を持たせる。

```python
def _restore_vad(self, voice, generation: int) -> None:
    # 800ms前と同じインスタンス・同じ世代のときだけ解除する
```

800ms後に「自分が記録したときと世代が同じか」を確認し、違えば**何もしない**。この「世代番号で古い処理を無効化する」パターンは、このアプリ全体で繰り返し使われています。

---

### ⑦ 停止ボタン ― 「止まったふり」をしない

⏹ボタンの処理（`Controller.stop`、381行）は、正直さが際立っています。

```
1. 中断フラグを立てる
2. TTSを止める、アバターの口を閉じる、マイクの排他を解除する
3. LLMのストリーミングループに中断を要求する
4. 別スレッドで「本当に止まったか」を0.1秒ごとに確認する
```

普通のアプリなら、ここで「停止しました」と表示して終わりです。しかしこのコードは**3秒待っても止まらなかった場合**に、こうします。

```python
self._app._chat_write(
    "\n⚠ LLMを3秒以内に停止できませんでした。"
    "安全のため操作をロックしています。"
    "アプリを終了して再起動してください。\n", "err")
```

**画面を操作可能に戻しません**。「止まったことにして先に進む」と、内部でLLMが動いたまま次の推論を始めてしまい、メモリ破壊やクラッシュにつながるからです。安全側に倒して、正直に「止められませんでした」と言う。地味ですが、なかなかできない判断です。

そして、もし遅れて本当に止まった場合は、世代番号を照合したうえでロックを解除します。

---

### ⑧ 会話履歴の暗号化と「原子的な書き込み」

**暗号化**: Windows標準の **DPAPI**（Data Protection API）を使っています。

```python
win32crypt.CryptProtectData(data, None, None, None, None, CRYPTPROTECT_UI_FORBIDDEN)
```

DPAPIの特徴は**鍵の管理が不要**なこと。Windowsが現在のユーザーアカウントに紐づいた鍵で暗号化してくれます。

- **利点**: パスワードを覚える必要がない。同じPCの別ユーザーは読めない。ファイルを他人のPCにコピーしても読めない。
- **欠点**: Windowsのユーザープロファイルが壊れる／再インストールすると、**自分でも二度と読めなくなる**。

このトレードオフはREADMEにも明記され、ヘルプメニューにも「履歴の暗号化と復旧」という項目が用意されています。

保存されるファイルはこういう形です。

```json
{"format":"llm-local-chat-dpapi","version":1,"ciphertext":"AQAAANCMnd8BFdER..."}
```

**原子的な書き込み**（`atomic_io.py`）: ファイルを上書きしている最中に停電やクラッシュが起きると、中途半端な壊れたファイルが残ります。それを防ぐ定番の手法です。

```
1. 同じフォルダに一時ファイルを作る（.chat_xxx.json.abc123.tmp）
2. そこに全部書く
3. flush() してから fsync()  ← OSのキャッシュではなく実際にディスクへ書かせる
4. os.replace() で名前を差し替える  ← ここは中断されない1つの操作
```

**ポイントは「同じフォルダ」**。別ドライブだと `os.replace` が単なるコピー＋削除に落ちてしまい、原子性が失われます。

---

### ⑨ 持ち出し用エクスポート ― パスフレーズ方式

DPAPI暗号は同じPCの同じユーザーでしか読めません。では別のPCへ会話を移したいときは？ それが `portable_history.py` です。

```
パスフレーズ（12文字以上）
   ↓ scrypt（N=32768, r=8, p=1）でゆっくり鍵に変換
32バイトの鍵
   ↓ AES-256-GCM で暗号化（ヘッダも改ざん検知の対象に含める）
.shiro-export ファイル
```

**scrypt を使う理由**: パスフレーズをそのまま鍵にすると、総当たり攻撃で秒間何百万回も試されます。scrypt は**わざと時間とメモリを食う**変換なので、攻撃者は1回試すのに時間がかかります。`N=32768` はその重さの設定です。

**AES-GCM を使う理由**: 暗号化と同時に「改ざんされていないこと」も保証してくれます。パスフレーズが違う場合も改ざんの場合も、`InvalidTag` という同じ例外になり、利用者には「パスフレーズが違うか、暗号化ファイルが破損しています」と表示されます（どちらか分からないようにするのは、攻撃者に手がかりを与えないためのセオリーです）。

**読み込み時の検証がとにかく細かい**（`import_archive`）:

```python
if not isinstance(envelope, dict) or set(envelope) != {
    "format", "version", "kdf", "cipher", "ciphertext"
}:
    raise PortableHistoryError("暗号化ファイルの形式が不正です。")
```

キーの集合が**完全一致**でなければ拒否。余計なキーが1つでもあれば通しません。さらに上限も細かく設定されています。

```
アーカイブ最大 64MB / 復号後 128MB / 会話 10,000件 / ターン 100,000 / 1文 400万字 / 入れ子の深さ 32
```

これは「爆弾ファイル」（小さいのに展開すると巨大化する細工ファイル）でメモリを食い潰される攻撃への対策です。

---

### ⑩ 外部連携の多層防御

家計簿と健康記録の登録は、このアプリで**唯一データが外に出ていく経路**です。しかもその内容は**AIが生成したもの**。信用できません。そこで防御が5層あります。

```
【第1層】送信先の制限
   http のみ / localhost・127.0.0.1・::1 のみ / ポート 8767・8766 のみ
   URLにユーザー名・パスワードが埋まっていたら拒否
   → https すら禁止。https を許すと外部サーバへ送れてしまうため

【第2層】キーのホワイトリスト
   決められた12個のキー以外は、その場で捨てる
   user_id, token, url, headers といった危険なキーはここで消える

【第3層】型と範囲の検証
   体温     34.0〜42.0 ℃
   体重     0〜300 kg（0は含まない）
   体脂肪率 0〜100 %
   脈拍     30〜200 bpm
   血圧上   50〜250 mmHg
   血圧下   30〜150 mmHg
   基礎代謝 0〜5000 kcal
   文字欄は 2000字まで
   bool・文字列の数字・NaN・Infinity・辞書・配列 が混ざったら、レコード全体を拒否

【第4層】ユーザーの原文を優先
   「食事ログ コーヒー」と書いたなら、その文字列をそのまま採用する
   AIが言い換えた値より、人間が書いた値を優先する

【第5層】確認ダイアログ
   整形した内容を見せて「はい／いいえ」。「はい」のときだけ送信
```

さらに送信時にも:

- `user_id` は**AIの出力を使わず、固定値 `"self"` を送信時に付ける**
- リダイレクト禁止（`_NoRedirectHandler`）― 転送されて外部に飛ぶのを防ぐ
- プロキシ無効化（`ProxyHandler({})`）― プロキシ経由で外に出るのを防ぐ
- タイムアウト5秒
- 応答は1MBまでしか読まない

**第4層の背景**: 健康記録モードで「体脂肪率17.9%」と言ったときに、AIが `activity_log`（行動ログ）にも「体脂肪率17.9%」を勝手にコピーしてしまう不具合がありました。プロンプトで禁止しただけでは直りきらなかったため、**コード側で「測定値の文だけからなる行動ログは無効にする」判定**（`_is_measurement_only_text`）を入れています。AIの出力を信用しきらない、という姿勢です。

---

## 7. データはどこに置かれるか

| 何 | どこ | 形式 | いつ消えるか |
|---|---|---|---|
| 設定 | `chat_settings.json` | 平文JSON | 自動削除なし |
| 会話履歴 | `chat_logs\chat_日時.json` | **DPAPI暗号化** | 個別削除、または保存期間超過時 |
| 要約メモリ | 上記ファイルの中 | 暗号化の中の文字列 | 会話ごと消える |
| テキスト書き出し | 利用者が指定した場所 | **平文 .txt** | 自動削除なし（毎回警告が出る） |
| 持ち出しアーカイブ | 利用者が指定した場所 | **AES-256-GCM** | 自動削除なし |
| アバター画像 | `avatars\` | PNG | アプリは書き込まない |
| Whisperモデル | `~\.cache\whisper` | モデルファイル | アプリは削除しない |
| ログ | **ファイルなし**（標準出力のみ） | — | — |

**保存期間**（`history_retention_days`）は 0（無期限）／30／90／180／365日から選べます。有限値にした場合も、**現在開いている会話と、復号できなかったファイルは削除対象から除外**されます。

**ゲストモード**では `_save_now` の1行目で処理を打ち切るので、ファイルは一切作られません。

---

## 8. 設定ファイルの中身

`chat_settings.json`（アプリと同じフォルダ）。設定画面から変えられるものと、ファイルを直接編集するしかないものがあります。

| キー | 既定値 | 意味 | 設定画面 |
|---|---|---|---|
| `model_path` | `models\gemma-3-4b-it-q4_k_m.gguf` | 使うAIモデル | ○ |
| `n_ctx` | 8192 | 一度に読める量 | ○ |
| `max_tokens` | 1024 | 返事の最大長 | ○ |
| `temperature` | 0.7 | 返事のばらつき（0=堅実、2=奔放） | ○（0.0〜2.0） |
| `tts_enabled` | false | 起動時に読み上げON | ○ |
| `tts_rate` | 0 | 読み上げ速度（-10〜+10） | ○ |
| `mic_enabled` | false | 起動時にマイクON | ○ |
| `whisper_mode` | auto | 音声認識の重さ | ○（要再起動） |
| `vad_threshold` | 150 | マイクの感度 | ○ |
| `history_retention_days` | 0 | 履歴の保存日数 | ○ |
| `n_threads_batch` | 12 | 計算スレッド数 | ✕ |
| `n_batch` | 1024 | バッチサイズ | ✕ |
| `n_ubatch` | 512 | マイクロバッチ | ✕ |
| `flash_attn` | true | 高速化手法 | ✕ |
| `offload_kqv` | true | GPU割り当て | ✕ |
| `use_mmap` | true | メモリマップ読込 | ✕ |

**堅牢性の作り**: ファイルが無い／JSONが壊れている場合、エラーを出さず**全部既定値で起動**します（`load_settings` の `except: pass`）。さらに性能系6キーは値の妥当性を検査し、おかしければ既定値に差し替えます（`_valid_positive_int` / `_valid_bool`）。「設定ファイルをいじって起動しなくなる」事故を防ぐ設計です。

---

## 9. テストについて

`tests\` フォルダに **20ファイル・約4,600行・テスト関数391個**があります。本体コードの半分近い分量です。

**2026-08-17 時点で391件すべて成功しています**（実行時間 3.17秒）。標準ライブラリの `unittest` を使うため、追加のテストライブラリは不要です。

```powershell
python -m unittest discover -s tests -t . -p "test*.py"
```

`.venv` がプロジェクト直下にあるため、`-s tests -t .` で収集範囲を限定しないと `.venv` 内の依存パッケージのテストまで拾ってしまう点に注意してください。

特に厚いのはこの3つ。

| テスト | 行数 | 何を確かめているか |
|---|---|---|
| `test_llm_concurrency.py` | 990 | 複数スレッドが同時にLLMを触ろうとしたときの衝突 |
| `test_streaming_tts.py` | 676 | 流れてくる文字の切り分けとコードブロック除外 |
| `test_biolog_sanitization.py` | 279 | 健康記録の危険な値をきちんと弾けているか |
| `test_portable_ui.py` | — | 暗号化エクスポート／インポート画面の挙動 |

**注目**: テストの重心が「機能が動くか」ではなく、**「壊れ方が正しいか」**に置かれています。異常な値、同時実行、途中での中断、パスフレーズ間違い ― こういう場面で**安全側に倒れるか**を確かめるテストが大半です。

---

## 10. 開発の歴史（バージョン履歴の要約）

`CHANGELOG.md` から流れを読み取ると、はっきりした3つの段階があります。

### 第1期（〜v1.1）― 機能を作る
チャット、音声入力、TTS、アバター、健康記録連携。まず動くものを作る時期。

### 第2期（v1.2〜v1.3）― 安定させる
`resource_monitor.py` によるVRAM安全フィルタ（v1.2.0）。「クラッシュしないこと」が主題になります。同時期に、1つの巨大ファイルだったものを `controller.py` / `llm_service.py` / `prompt_builder.py` などへ分割するリファクタリングが行われています。

### 第3期（v1.4〜v1.6）― 守りを固める
ここからは**ほぼすべてがセキュリティと堅牢性**の変更です。

| 版 | 主な変更 |
|---|---|
| v1.4.0 | 大規模な整理 |
| v1.4.2 | **履歴のDPAPI暗号化を導入**。既存の平文JSONを起動時に自動移行 |
| v1.4.3 | ローカルAPI制限の強化 |
| v1.5.0 | **パスフレーズ付き暗号化エクスポート／インポート**を追加 |
| v1.5.1 | ゲスト会話・未保存会話がエクスポートできてしまう不具合を修正 |
| v1.5.2 | エクスポート画面のUI改善、CPU専用環境向け `requirements-cpu.txt` を追加 |
| v1.6.0 | 家計簿ブリッジのポートを 8765 → **8767** へ変更（8765を別サービスが使っていたため） |

CHANGELOG には修正だけでなく**訂正**も記録されています。

> v1.5.1で回帰テスト3件を追加したとする不正確な記載を訂正

自分の変更履歴の誤りまで直して残している。これは記録として珍しい丁寧さです。

---

## 11. 読んでいて気づいた点

コードを読んで確認できた事実のみを挙げます。**いずれも動作の不具合ではありません**が、把握しておくと混乱を避けられます。

> **対応済みのため本節から除いた項目**（2026-08-17）
>
> 当初7項目を挙げていましたが、次の5件は対応が完了したため削除しました。
>
> - v1.6.0 のバージョン反映漏れ（`APP_VERSION`、READMEバッジ、README版別節） → すべて 1.6.0 に更新
> - ルート直下に残っていた旧テスト14件 → 退避のうえ削除
> - 現行コードから参照されていない設定ファイル3件（`data\config.json`、`advanced_settings.json`、`records_after.json`） → 退避のうえ削除
> - `chat_settings.json` の未使用キー2件（`vram_danger_gpu_pct` / `vram_danger_vram_pct`） → 削除
> - `.bak` ファイル7件 → 退避のうえ削除
>
> 退避先は `D:\AI\LLM\LLM_Local_Chat_removed_20260817\`（SHA-256照合済み）。以下は現時点で**未解決、または対応に判断が必要**な項目です。

### ① モデルファイルのハッシュ一覧が無い

README の「動作確認済みモデル一覧」（342行〜）は次の4列だけです。

| モデル名 | パラメータ | 備考 | ダウンロード |
|---|---|---|---|

**SHA-256 などのチェックサム欄がありません。** ダウンロードした `.gguf` が正しいファイルかを利用者が検証する手段は文書化されておらず、「信頼できる配布元から」という記述のみです（332行）。

実害は限定的です。GGUFが破損していれば `init_llm` が例外を投げて「❌ モデル読込失敗」になるため、黙って誤動作することはありません。ただし**別の量子化版を取り違えて入手した場合や、ダウンロード途中で切れた場合を事前に検出できません**。また**作者の開発機の** `models\` にある3ファイル（gemma-4-E4B-Q4_K_M / gemma-4-E4B-it-Q4_0 / granite-4.1-3b-Q4_K_M）のうち、READMEの一覧に載っているのは gemma-4-E4B-it 系だけで、`granite-4.1-3b` と `gemma-4-E4B-Q4_K_M` は一覧に記載がありません。なお、これらのモデル本体はいずれもリポジトリには含まれていません（5-3節）。

なお**アプリ内部ではSHA-256を2か所で使っています**（README未記載）。

| 場所 | 用途 |
|---|---|
| `session_store.py` 44行 `_file_signature` | 履歴一覧キャッシュの無効化判定（内容ハッシュ＋日時＋サイズ） |
| `portable_history.py` 122行 `session_digest` | エクスポート／インポート時の会話の重複判定 |

「ハッシュを使う仕組みが無い」のではなく、**使ってはいるが配布物の完全性検証には使っていない**、という状態です。

### ② 実行環境（D:）の Git 未コミット変更

2026-08-17 時点で、変更済み18件・削除済み2件・未追跡14件です。未追跡には `LICENSE`、`SECURITY.md`、`Start_Shiro.bat`、`atomic_io.py`、`history_crypto.py`、`portable_history.py`、`kakeibo_*.py` などが含まれます。

これは**このフォルダが実行環境だから**です。同じプロジェクトが3か所に存在し、git の起点は開発環境（`C:\Users\shino\python-mysql-dev\app\LLM_Local_Chat`）、公開は公開環境（`D:\AI\github_upload\LLM_Local_Chat`）が担っています。3拠点の役割と同期ルールは `CLAUDE.md` の「3拠点構成」節にまとめられています。

**判断が必要な点**: 実行環境側のコミットをどう扱うか（開発環境へ集約するのか、実行環境も独立してコミットするのか）が未整理です。現状は実行環境のコミットが `000d529` で止まっており、公開環境（`62c16d0`）と履歴が大きく乖離しています。

### ③ GUI本体の実機動作確認は未実施

**回帰テストは通っています。** 2026-08-17 に実行環境の `.venv` で `python -m unittest discover -s tests -t . -p "test*.py"` を実行し、**391件すべて成功**（3.17秒）しました。VRAM判定、Whisper品質ゲート、TTSストリーミング、Biologサニタイズ、暗号化エクスポートなどが自動検証されています。

一方で、次のものは**依然として実機確認されていません**。自動テストではモックに置き換わっている部分です。

- GUIの起動と画面操作（Tkinterウィンドウ、設定画面、各ボタン）
- 実際のGGUFモデルのロードと推論
- マイク入力からWhisper認識までの一連の流れ
- SAPI5による実際の読み上げとアバターの口パク連動
- 家計簿・Biolog APIへの実POST（Dockerサービス稼働が前提）

`docs/CURRENT_SPEC.md` §13 にも同様の記載があります。これらは実機のGPU・マイク・スピーカー・Dockerが揃った環境で人が操作して確認する必要があります。

---

## 12. 用語集

| 用語 | 意味 |
|---|---|
| **LLM** | 大規模言語モデル。次の単語を予測して文章を作るAI |
| **GGUF** | llama.cpp が読み込むモデルファイル形式。1ファイルで完結 |
| **llama.cpp** | GGUFを動かすエンジン。CPUでもGPUでも動く |
| **量子化 (Q4_K_M など)** | モデルの精度をわずかに落とす代わりにファイルを小さくする技術。Q4は約4ビット相当 |
| **トークン** | AIが扱う文字のかたまり。日本語で1〜2文字程度 |
| **コンテキスト長 (n_ctx)** | 一度にAIが読める最大トークン数。このアプリの既定は8192 |
| **temperature** | 返事のばらつき。0に近いほど堅実、高いほど奔放 |
| **ストリーミング** | 全部できてから出すのではなく、できた分から順に出す方式 |
| **VRAM** | GPU専用のメモリ。足りないとアプリが落ちる |
| **OOM** | Out Of Memory。メモリ不足によるクラッシュ |
| **VAD** | Voice Activity Detection。音量から「今喋っている」を判定する仕組み |
| **RMS** | 音量の指標（二乗平均平方根）。VADの判定に使う |
| **Whisper** | OpenAIの音声認識モデル。ローカルで動かせる |
| **ハルシネーション** | AIがもっともらしい嘘を作ること。Whisperでは無音から文章をでっち上げる現象 |
| **TTS** | Text To Speech。文字を音声にする |
| **SAPI5** | Windows標準の音声合成API |
| **Tkinter** | Python標準のGUI部品集。追加インストール不要 |
| **スレッド** | 1つのプログラム内で同時進行する作業の単位 |
| **デーモンスレッド** | 本体が終了したら道連れで終わるスレッド |
| **キュー** | 順番待ちの行列。TTSの読み上げ待ちに使用 |
| **LRUキャッシュ** | 使われていないものから捨てるキャッシュ方式 |
| **ヒステリシス** | 切替の境界に幅を持たせて、境界付近でのバタつきを防ぐ手法 |
| **DPAPI** | Windowsの暗号化API。ユーザーアカウントに紐づく鍵を自動管理 |
| **AES-256-GCM** | 標準的な暗号方式。暗号化と改ざん検知を同時に行う |
| **scrypt** | パスワードから鍵を作る関数。わざと時間とメモリを食い、総当たり攻撃を遅くする |
| **原子的 (atomic)** | 「全部成功」か「何もしない」のどちらかにしかならない性質 |
| **サニタイズ** | 危険な値を取り除いて安全な形に整えること |
| **ホワイトリスト** | 「許可するものだけを列挙する」方式。逆はブラックリスト |
| **多層防御** | 1つの対策に頼らず、独立した防御を何段も重ねる考え方 |
| **回帰テスト** | 一度直した不具合が再発していないかを確認するテスト |
| **venv** | Pythonの隔離実行環境。プロジェクトごとにライブラリを分ける |
| **Docker / docker compose** | アプリを箱に入れて動かす仕組み。家計簿とBiologのDBがこれで動く |

---

## 最後に ― このコードの性格

全体を読んで一番強く感じるのは、**「うまく動くこと」より「まずいことにならないこと」を優先している**という点です。具体例を挙げると:

- モデル読込は**最大6回**リトライする（起動できないのが一番困るから）
- VRAMが足りなければ、動かないのではなく**生成量を減らして動く**
- 停止できなかったら、**嘘をつかず「止められませんでした」と言って画面をロックする**
- 暗号化したら、**その場で復号して照合してから**保存する
- AIの出した値は**5層のチェックを通さないと**外部へ送らない
- 設定ファイルが壊れていたら、エラーで止まらず**既定値で起動する**

そしてその判断の根拠が、コード中のコメントや `CHANGELOG.md`、`docs/CURRENT_SPEC.md` に文章として残されています。「閾値1500に対してTTS音声のRMSが2530だった」といった**実測値まで書き残されている**のは、個人開発では珍しい水準です。

技術用語を取り払って言えば、これは「自分のPCの中だけで完結する、声で話せるAIアシスタント」を、**壊れても被害が最小で済むように、何重にも保険をかけながら作った**アプリです。

---

*この文書は 2026-08-17 時点の `D:\AI\LLM\LLM_Local_Chat` のコードを読んで作成しました。コードは読み取りのみで、変更・実行は行っていません。*

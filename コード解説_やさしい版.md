# LLM Local Chat ― コード解説（やさしい版）

作成日: 2026-08-17 / 全文再確認: 2026-08-18  
対象: LLM Local Chat v1.6.3 の現行コード

この文書は、**現在のコードから確認できる動作を、最近のAI周辺技術から離れていた方にも追いやすい形で説明する資料**です。

記述にあたっては、次の順序で根拠を扱います。

1. Pythonコード、設定テンプレート、起動バッチ
2. 回帰テスト
3. README / CHANGELOG などの公開文書

コードと文書の記載が食い違う場合は、現行コードを優先します。行番号・総行数・テスト件数など、更新で変わりやすい値は原則として本文へ固定しません。

> **重要**
>
> リポジトリをクローンしただけでは実行準備は完了しません。GGUFモデル、Python仮想環境、`chat_settings.json` は各自で用意します。音声入力を起動時から有効にする場合は、Whisperモデルの取得が発生することがあります。

---

## 目次

1. [このアプリは何をするものか](#1-このアプリは何をするものか)
2. [実行前に別途必要なもの](#2-実行前に別途必要なもの)
3. [全体構成](#3-全体構成)
4. [起動から返答までの流れ](#4-起動から返答までの流れ)
5. [主要ファイルの役割](#5-主要ファイルの役割)
6. [LLMの読み込み・履歴・排他制御](#6-llmの読み込み履歴排他制御)
7. [VRAM監視とWhisper](#7-vram監視とwhisper)
8. [音声入力・読み上げ・アバター](#8-音声入力読み上げアバター)
9. [会話履歴の保存と暗号化](#9-会話履歴の保存と暗号化)
10. [持ち出し用暗号化エクスポート](#10-持ち出し用暗号化エクスポート)
11. [家計簿・健康記録の連携](#11-家計簿健康記録の連携)
12. [設定ファイル](#12-設定ファイル)
13. [テストと未確認事項](#13-テストと未確認事項)
14. [用語集](#14-用語集)

---

## 1. このアプリは何をするものか

LLM Local Chat は、**GGUF形式のLLMをPC上で読み込み、日本語で対話するWindows向けデスクトップアプリ**です。GUIはTkinter、LLM実行は `llama-cpp-python` を使用します。

主な機能は次のとおりです。

| 機能 | 実装上の概要 |
|---|---|
| テキストチャット | ローカルのGGUFモデルへ入力を渡し、応答をストリーミング表示 |
| 会話履歴 | `chat_logs/` にセッション単位で保存 |
| 要約メモリ | 一定ターンごとに短い要約を作り、以後のプロンプトへ追加 |
| 音声入力 | PyAudio + Whisper + RMSベースVAD |
| 読み上げ | Windows SAPI5 |
| アバター | 瞬きとTTS連動の口パク |
| 家計簿連携 | ユーザー確認後、localhost上の家計簿APIへPOST |
| 健康記録連携 | ユーザー確認後、localhost上のBiolog APIへPOST |
| 履歴の持ち出し | scrypt + AES-256-GCM を使うパスフレーズ方式 |

### 処理が行われる場所

LLM推論、Whisperによる音声認識、SAPI5による音声合成、通常の会話処理はローカルで実行されます。本体コードには、会話本文をインターネット上の生成AIサービスへ送る処理は確認できません。

一方、次の点は区別が必要です。

- Whisperモデルは `whisper.load_model()` で読み込まれるため、ローカルにモデルが無い状態では取得処理が発生することがあります。
- 家計簿・健康記録は別プロセスのローカルAPIへデータを渡します。
- 依存ライブラリやOS全体の通信動作まで、この文書が網羅して保証するものではありません。

### 会話履歴の暗号化について

通常の保存済み会話はWindows DPAPIで保護されます。ただし、**同じWindowsユーザーの権限で動くプログラムからの復号を防ぐ仕組みではありません**。DPAPIは「そのユーザー環境で扱うデータの保護」に使われており、アプリ独自のパスフレーズを毎回要求する方式ではありません。

別PCへ履歴を移す用途には、後述するパスフレーズ付き暗号化エクスポートを使います。

---

## 2. 実行前に別途必要なもの

### リポジトリに含まれないもの

| 対象 | 説明 |
|---|---|
| GGUFモデル本体 | `.gitignore` で `models/` と `*.gguf` が除外されているため、各自で取得して配置 |
| `.venv/` | Python仮想環境。各自で作成 |
| `chat_settings.json` | `chat_settings.json.example` を基に作成 |
| Whisperモデル | 音声入力用。必要時にWhisperライブラリが読み込む |
| 家計簿側サービス | 別プロジェクト。2026-08-18時点の公開文書では家計簿側は非公開と記録 |
| Biolog | 健康記録用の別プロジェクト |

リポジトリ内の `models/ここにモデルを追加してください.txt` は配置場所を示す空ファイルで、GGUFモデル本体ではありません。

### `chat_settings.json.example` の意味

テンプレートでは `model_path` が `C:\path\to\your\model.gguf` になっています。これは利用者が実際のモデルパスへ書き換えるためのプレースホルダーです。

アプリ本体には別に内部既定値 `models\gemma-3-4b-it-q4_k_m.gguf` が定義されています。**テンプレート上の値と、設定ファイルが無い場合の内部既定値は別物**です。

---

## 3. 全体構成

処理の中心は `LLM_Local_Chat.py` の `ChatApp` ですが、生成処理・プロンプト構築・VRAM判定・保存処理などは別モジュールへ分離されています。

```text
LLM_Local_Chat.py
  ChatApp / SettingsDialog / AvatarWindow / 各種UI
          |
          v
controller.py
  入力・モード・生成・停止・要約の流れを調整
          |
          +--> prompt_builder.py
          |      LLMへ渡すmessagesと各モード用プロンプトを構築
          |
          +--> resource_manager.py
          |      推論前のmax_tokens判定をresource_monitorへ委譲
          |
          +--> llm_service.py
          |      LLM生成・要約・健康データ再抽出を実行
          |
          +--> json_extractors.py
                 LLM応答から構造化JSON候補を抽出

保存系
  session_store.py
      +--> history_crypto.py   DPAPI
      +--> atomic_io.py        一時ファイル経由の置換
  portable_history.py         パスフレーズ付き持ち出し形式

音声・リソース系
  audio_workers.py            TTS / マイク / VAD / Whisper呼び出し
  resource_monitor.py         VRAM観測 / LLM・Whisper配置判断

外部連携
  integrations.py             localhost API制限・Biolog検証・POST
  kakeibo_amount.py           ユーザー原文から金額抽出
  kakeibo_date.py             ユーザー原文から日付抽出
  kakeibo_confirmation.py     家計簿候補と送信直前検証
```

`controller.py` 自身の冒頭コメントは、Controllerを「判断のみ行い、実行は各サービスに委譲する薄いオーケストレーター」と位置付けています。

---

## 4. 起動から返答までの流れ

### 4-1. Windowsの起動バッチ

同梱の `LLMローカル対話型AI.bat` は、次の処理を行います。

```text
1. コンソールをUTF-8コードページへ変更
2. バッチファイル自身のフォルダへ移動
3. .venv\Scripts\activate.bat を呼ぶ
4. python LLM_Local_Chat.py を実行
5. 終了後に pause
```

仮想環境名を変えた場合は、バッチ内のパスも変更する必要があります。

### 4-2. Python側の起動

`main()` は概ね次の順序です。

1. `tk.Tk()` を作り、最初はウィンドウを非表示にする
2. `create_app_deps()` で `ResourceMonitor`、`WhisperPool`、`SessionStore` を作る
3. `ChatApp` を初期化する
4. 会話履歴の安全確認と、必要なら旧平文履歴の移行を行う
5. アバター、TTSワーカー、UI、Controllerを作る
6. `_reload_llm()` からLLM読み込みをバックグラウンドスレッドで開始する
7. `ChatApp` 初期化後にウィンドウを表示し、`mainloop()` に入る

履歴のスキャンで破損・未対応形式が検出された場合や、必要な移行が完了しなかった場合は、通常起動を中止する経路があります。

### 4-3. Whisperの起動条件

Whisperの初回ロードは、LLMのロード処理が終わった後に開始されます。

さらに `should_load_whisper()` は、設定の `mic_enabled` が明示的に `true` の場合だけWhisperを必要と判定します。したがって、既定値 `false` のまま起動した場合はWhisperをロードしません。

起動時にWhisperを読み込まなかった状態から音声入力を使う場合、UIは再起動が必要である旨を案内する実装になっています。

### 4-4. メッセージ送信

`Controller.handle_text()` が通常の入口です。

```text
入力
  ↓
処理中か確認
  ↓
LLMが準備済みか確認
  ↓
通常 / 家計簿 / 健康記録 のモードを決定
  ↓
PromptBuilderでmessagesを構築
  ↓
ResourceManagerで今回のmax_tokensを決定
  ↓
LLMService.generate()を開始
  ↓
文字列断片が届くたびUIへ反映
  ↓
完了後に履歴・連携候補・要約を処理
```

LLMから届く `choice["delta"]["content"]` は文字列断片であり、1回のコールバックが1文字とは限りません。

### モードごとの差

**通常モード**では、応答をそのまま表示し、保存対象の会話履歴へ追加します。

**健康記録モード**では、先頭に出る健康JSONを `LeadingJsonFilter` で表示対象から外し、後続の自然文を表示します。生成後は健康データを検証し、必要なら健康データ抽出専用の再生成を行います。

**家計簿モード**では、LLM応答は家計簿候補の抽出に使いますが、通常のAI返答としてストリーミング表示・読み上げを行いません。また、この家計簿モードのやり取りは通常会話履歴へ追加しない実装です。

---

## 5. 主要ファイルの役割

| ファイル | 現行コードでの主な役割 |
|---|---|
| `LLM_Local_Chat.py` | GUI、設定、モデルロード、アバター、履歴UI、暗号化履歴UI |
| `app_composition.py` | `ResourceMonitor` / `WhisperPool` / `SessionStore` をまとめて生成 |
| `controller.py` | 生成開始・モード・停止・要約・健康再抽出などの処理順を制御 |
| `llm_service.py` | LLM実行専用。生成・健康抽出・要約を単一実行状態で管理 |
| `prompt_builder.py` | system/user/historyをmessagesへまとめ、家計簿・健康用プロンプトも作成 |
| `resource_manager.py` | `adjust_inference()` を呼び、推論時の生成上限を決定 |
| `resource_monitor.py` | GPU/VRAM観測、LLMのGPU/CPU判断、Whisperモデル管理 |
| `audio_workers.py` | SAPI5 TTS、マイク入力、RMS-VAD、Whisper認識品質判定 |
| `session_store.py` | 保存・一覧・読込・削除・改名・移行・保存期限・インポート |
| `history_crypto.py` | DPAPIエンベロープの暗号化・復号 |
| `atomic_io.py` | 同一ディレクトリの一時ファイルを書き終えてから `os.replace()` |
| `portable_history.py` | scrypt + AES-256-GCM の持ち出し用履歴形式 |
| `json_extractors.py` | LLM応答から家計簿JSON候補・健康JSON候補を抽出 |
| `kakeibo_amount.py` | ユーザー原文から1件の正の整数金額を機械抽出 |
| `kakeibo_date.py` | ユーザー原文から対応形式の日付を抽出 |
| `kakeibo_confirmation.py` | 家計簿確認候補の構築とAPI送信直前の検証 |
| `integrations.py` | ローカルAPI送信先制限、Biolog検証、非同期POST |
| `check_audio_devices.py` | 入力デバイス一覧と短時間RMS測定を行う独立ツール |

### `json_extractors.py` の注意点

健康JSONは `_strict_json_loads()` を使い、`NaN` / `Infinity` のような非標準定数を拒否します。

家計簿JSON候補の抽出は通常の `json.loads()` を使用します。ただし、家計簿の最終 `amount` と `date` はLLM出力から確定せず、後段でユーザー原文から機械抽出します。

### `kakeibo_date.py` が対応している日付

現行コードで明示的に扱っているのは次の形式です。

- `2026/08/16`
- `2026-08-16`
- `7/17` のような月/日形式

月/日形式では実行年を補います。実在しない日付、未対応形式、日付表現が無い入力は実行日にフォールバックします。

**「おととい」「先週の金曜」などの自然言語相対日付を解析するコードはありません。**

### `kakeibo_amount.py` が扱う金額

ユーザー原文の数字を半角化した上で、`円` / `千円` / `万円` を伴う正の整数金額を抽出します。

例:

- `1500円`
- `1,500円`
- `2万円`
- `5千円`

一方、現行コードは小数、符号付き金額、指数表記、数字途中の空白、複合単位表記などを不正形式として扱います。`1万5千円` のような複合単位表記も拒否対象です。

有効な金額候補が複数ある場合は `multiple_amounts` とし、1回の入力で複数取引を登録しません。

---

## 6. LLMの読み込み・履歴・排他制御

### 6-1. モデル読み込みのフォールバック

`init_llm()` はまず `adjust_llm()` の結果を使って、GPUオフロードを使うかCPU実行にするかを決めます。

GPUを選択した場合、既定構成では次の順で読み込みを試します。

1. 性能設定を含む構成
2. `flash_attn=False` にした構成
3. 基本引数だけの構成

GPU側で読み込めなかった場合はCPU側でも同様の候補を試すため、**既定設定では最大6回のロード試行になる場合があります**。最初からCPU判定になった場合などは、試行回数は少なくなります。

CPUへ切り替える前には `gc.collect()` と、利用可能な場合の `torch.cuda.empty_cache()` を呼びます。

この仕組みは「必ずロードできる」ことを保証するものではなく、すべて失敗した場合は最後の例外を上位へ返します。

### 6-2. 会話履歴をコンテキストへ入れる量

`PromptBuilder.build()` は、履歴用の予算を次の考え方で計算します。

```text
残り予算 = n_ctx - max_tokens - system分 - 今回の入力分
履歴へ割り当てる予算 = 残り予算 × HISTORY_BUDGET_RATIO
```

現行の `HISTORY_BUDGET_RATIO` は `0.60`、system側には追加で `SYSTEM_BUF_TOKENS = 256` を見込みます。

履歴は新しい会話ペアから逆順に見て、予算へ収まる範囲を採用し、最終的なmessagesでは元の時系列順へ戻します。各会話ペアにはコード上の固定見積もり `+12` が加算されます。

トークン数は原則としてモデルのtokenizerで数えます。`LLM_Local_Chat.py` の `count_tokens()` はtokenizer呼び出しが失敗した場合に文字数ベースのフォールバック値を返します。

### 6-3. 要約メモリ

通常会話と健康記録モードの会話は保存履歴へ追加され、履歴数が `SUMMARY_THRESHOLD` の条件を満たすと要約処理を開始します。現行値は4ターンです。

要約用LLM呼び出しは `LLMService.summarize()` が担当し、次の値を使います。

```text
max_tokens = 80
temperature = 0.3
stop = ["\n"]
```

通常生成の既定temperatureより低い値を使い、出力のばらつきを抑えています。完成した要約はセッションの `summary` へ保存され、次回以降のsystemメッセージへ `[要約]: ...` として追加されます。

家計簿モードは通常会話履歴へ追加されないため、この履歴数にも入りません。

### 6-4. LLMはキューではなく同時開始を拒否する

`LLMService` は `_state_lock` と `_running` を持ちます。

```python
with self._state_lock:
    if self._running:
        return False
    self._running = True
```

したがって、すでにLLM処理が走っているとき、新しいLLM処理を待ち行列へ積むのではなく**開始要求を拒否**します。

読み上げ側の `TTSWorker` は `queue.Queue` を使用するため、LLM側とは設計が異なります。

### 6-5. 世代番号で古いコールバックを無効化

Controllerは `_operation_generation` を増やし、ストリーミングや完了コールバックが現在の操作世代と一致するか確認します。

停止や新しい処理によって世代が変わった後に古いコールバックが届いた場合、その処理は現在の操作へ反映しません。

### 6-6. 停止ボタン

`Controller.stop()` は次の処理を行います。

1. LLM中断フラグを立てる
2. TTSキューと再生を停止方向へ切り替える
3. アバターの発話状態を戻す
4. マイク側のTTS排他状態を解除する
5. `LLMService.abort()` を呼ぶ
6. 別スレッドでLLM実行状態を監視する

3秒以内にLLMが停止状態へ移らない場合、UIへ警告を出し、「安全のため新しい推論は開始しない」と案内します。その後も監視は続き、実際にLLMが停止した時点で現在世代と一致すれば操作状態を解除します。

ここで行っているのは、停止未確認の状態で次の生成を開始しないための制御です。

---

## 7. VRAM監視とWhisper

### 7-1. `ResourceMonitor`

`ResourceMonitor` は生成時に作られ、デーモンスレッドでGPU・VRAM・CPUの診断値を更新します。ポーリング周期は `0.5` 秒です。

VRAMの即時判定では `snapshot()` が同期取得を行います。

GPU情報の取得は次の順に試します。

1. `pynvml`（`requirements.txt` 上のパッケージは `nvidia-ml-py`）
2. `nvidia-smi`
3. 取得できなければGPU情報なしとして扱う

### 7-2. LLMロード前

`adjust_llm()` は、GPUオフロード対応の有無、空きVRAM、GGUFファイルサイズを使って `n_gpu_layers` を決めます。

起動時の予約量は次の大きい方です。

```text
1024MB
総VRAMの12%
```

概算必要量は `モデルファイルサイズ + 起動予約量` です。条件を満たす場合は `n_gpu_layers=-1`、それ以外は `0` を返します。

### 7-3. 推論直前

`adjust_inference()` はLLMがGPU推論中の場合に、空きVRAMを見て `max_tokens` を調整します。

| 条件 | 現行動作 |
|---|---|
| 空きが最低予約量より少ない | 推論を開始しない |
| 空きが1024MB未満 | `max(256, default_max // 4)` |
| 空きが1536MB未満 | `max(256, default_max // 2)` |
| それ以上 | 設定値を維持 |

最低予約量は `512MB` と総VRAMの `6%` の大きい方です。

CPU推論時、またはGPUメモリ情報を取得できない場合は、GPU残量を理由にこのブロックを行いません。

Whisper側から渡された直近GPU使用率変化が `10` ポイントを超える場合は、さらに生成上限を半分へ調整します。

### 7-4. Whisperのモデル配置

`WhisperPool.load()` はまずCPU版 `small` をロードします。その後、設定と空きVRAMに応じてGPUモデルのロードも試します。

`auto` の選択基準は次のとおりです。

| 空きVRAM | GPU側の候補 |
|---|---|
| 4096MB以上 | `medium` |
| 2048MB以上 | `small` |
| それ未満 | GPUモデルを追加せずCPU `small` |

手動でGPUモデルを指定していても、必要な空き量に満たない場合はCPU `small` 側へフォールバックします。GPUモデルのロードに失敗した場合もCPUモデルを残します。

### 7-5. WhisperのGPU/CPU切替

GPUモデルが利用可能な場合、`WhisperController` はGPU使用率に対してヒステリシスを持ちます。

- GPU利用中に使用率が88%を超える → CPU側へ
- CPU利用中に使用率が70%を下回る → GPU側へ

88と70で境界を分けることで、単一のしきい値付近で切替が連続しにくい構造です。

`VRAMGuard` クラスも `resource_monitor.py` 内に定義されていますが、現行の他モジュールからこのクラスを生成・参照するコードは確認できません。実際の生成経路では `adjust_llm()` と `adjust_inference()` が使用されています。

---

## 8. 音声入力・読み上げ・アバター

### 8-1. 音声入力の既定値

`chat_settings.json.example` と `load_settings()` の両方で、`mic_enabled` の既定値は `false` です。

起動時に `mic_enabled=false` の場合、Whisperのロードをスキップします。

起動時にWhisperをロードした場合、`VoiceRecognizer` はバックグラウンドスレッドを開始します。マイクON/OFFは `_enabled` イベントで制御され、OFF中は認識処理へ進まず待機します。

したがって、「マイクスレッドはONにするたび作成・破棄する」という構造ではありません。

### 8-2. VAD

音声入力は16kHz、モノラル、1024サンプル単位で読み取ります。

発話開始は、RMSが設定しきい値を連続6チャンク超えた場合に確定します。プリロールとして直近0.75秒相当のチャンクを保持します。

録音開始後は、連続30チャンクの静音、または最大6秒の到達で録音区間を終えます。

### 8-3. Whisper認識結果のフィルタ

認識後は、複数の条件で結果を破棄することがあります。

- `no_speech_prob`
- `avg_logprob`
- `compression_ratio`
- 短いフレーズの反復
- 空文字
- 2文字未満
- 100文字超
- ノイズ語句の完全一致・部分一致
- 句読点だけの結果
- 10秒以内の同一文字列
- TTSが認識処理と重なった場合

100文字という値は、一般的な発話長の上限を示すものではなく、**この実装が誤認識抑制用に置いているヒューリスティック**です。

`compression_ratio` も「繰り返し量そのもの」ではなく、Whisperが返す認識区間の指標として扱われています。コードは他の指標と組み合わせて判定します。

### 8-4. TTSとの相互干渉

TTS開始時はマイク側へTTS中であることを通知します。`VoiceRecognizer` はTTS中のマイクチャンクを発話判定へ蓄積しません。

録音中にTTSが始まった場合は、その録音データを破棄してWhisper処理へ進まない経路があります。またWhisper処理の前後でもTTS世代を確認します。

TTS終了後のマイク抑制解除には世代番号を使い、新しいTTSが開始済みなら古い解除処理を適用しません。

### 8-5. 読み上げ

`TTSWorker` は専用デーモンスレッドと `queue.Queue` を持ちます。

- 通常の読み上げは `SAPI.SpVoice`
- `speaker.Speak(text, 1)` で非同期再生を開始
- 停止要求時には `speaker.Speak("", 3)` を呼ぶ

`tts_enabled` の既定値は `false` です。TTSワーカースレッド自体はUI初期化時に作られますが、`enabled=false` なら `speak()` は読み上げ要求をキューへ追加しません。

ストリーミング応答は `StreamingTTSBuffer` が `。！？!?` と改行を境界として文単位へ分割します。コードフェンス内の内容は読み上げ対象から外します。

### 8-6. アバター

`AvatarWindow` は別のTkウィンドウとして作られます。

- 瞬き間隔: 2〜6秒のランダム
- 瞬き時間: 150ms
- 口パク切替: 140ms
- ドラッグ移動
- 表示/非表示切替

画像読み込みに失敗した場合、`_load()` は透明な100×100画像を代わりに作ります。瞬き用画像が個別に無い場合は既存画像を使うフォールバックもあります。

---

## 9. 会話履歴の保存と暗号化

### 9-1. 保存先

`SessionStore` はアプリ配置フォルダ配下の `chat_logs/` を管理します。初期化時にこのディレクトリが無ければ作成します。

会話が空の場合は保存ファイルを作りません。保存ファイル名は概ね次の形式です。

```text
chat_YYYYMMDD_HHMMSS_ffffff.json
```

### 9-2. DPAPIエンベロープ

`history_crypto.py` の `DPAPIProtector` はWindowsの現在ユーザースコープで `CryptProtectData` / `CryptUnprotectData` を呼びます。

保存ファイルは平文セッションそのものではなく、概ね次のエンベロープになります。

```json
{
  "format": "llm-local-chat-dpapi",
  "version": 1,
  "ciphertext": "..."
}
```

保存時の `_write_encrypted()` は、

1. セッション形式を検証
2. JSON化
3. DPAPIで暗号化
4. その場で復号
5. 復号結果が元バイト列と一致することを確認
6. ファイルへ書き込む

という順序です。

一致しない場合は保存処理をエラーにします。DPAPI保存に失敗した場合、`ChatApp._save_now()` は平文へ自動フォールバックしません。

### 9-3. 一時ファイル経由の置換

`atomic_io.atomic_write_bytes()` は、対象と同じディレクトリに `NamedTemporaryFile` を作り、

```text
write
  ↓
flush
  ↓
os.fsync(file descriptor)
  ↓
os.replace(temp, target)
```

の順で処理します。

これは既存ファイルを先頭から直接上書きするのではなく、**一時ファイルを完成させてから対象パスへ置換する構造**です。

このコードはファイルディスクリプタに対する `os.fsync()` は呼びますが、置換後のディレクトリ自体を `fsync` する処理はありません。このため、この文書では電源断時の永続性まで保証する表現は使用しません。

### 9-4. 旧平文履歴の移行

起動時に `scan_legacy()` で `chat_logs/*.json` を確認します。

- 未対応・破損ファイルがある場合は通常起動を中止
- 平文履歴がある場合はユーザーへ確認
- DPAPIの事前往復テストを行う
- 移行用ロックファイルを作る
- 移行直前にも対象を再確認
- 読み込んだ元ファイルが処理中に変化していないことを時刻・サイズ・内容で確認
- 暗号化結果を復号して照合してから置換
- 移行後に再スキャン

という手順があります。

### 9-5. 履歴一覧キャッシュ

`SessionStore._file_signature()` は、次の4要素を署名として使います。

```text
mtime_ns
size
ctime_ns
SHA-256(ファイル全内容)
```

SHA-256はファイルを1MBずつ読んで毎回計算します。その署名が前回と同じ場合、後段のDPAPI復号とJSONパースを省略します。

つまりこのキャッシュは「ファイルを読まなくするキャッシュ」ではありません。**内容ハッシュの計算は行い、復号・パース結果の再取得を省略する仕組み**です。

### 9-6. 保存期間

`history_retention_days` は `0 / 30 / 90 / 180 / 365` のみを受け付け、既定値0は無期限です。

有限値の場合、`last_activity_at` を基準に期限候補を判定します。現在開いているセッションや、読み取りに失敗するファイルを無条件に削除する処理にはなっていません。

### 9-7. ゲストモード

ゲストモードでは `ChatApp._save_now()` が保存処理へ進まず戻ります。したがって**ゲスト会話の会話履歴ファイルは保存しません**。

ただし、`SessionStore` 初期化時の `chat_logs/` 作成など、アプリ全体が一切ファイルを作らないという意味ではありません。

### 9-8. 平文テキスト保存

現行の `LLM_Local_Chat.py` の「ファイル」メニューには、

- 新規チャット
- 保存
- 暗号化エクスポート
- 暗号化インポート
- 設定
- 終了

があり、**旧版にあった平文の「テキストとして保存」は現行コードにはありません**。CHANGELOGでもv1.5.0で暗号化エクスポートへ置き換えたことが記録されています。

---

## 10. 持ち出し用暗号化エクスポート

通常の保存履歴はDPAPIで保護されるため、別PCへの持ち出しには `portable_history.py` の形式を使います。

### 10-1. 暗号化方式

```text
12文字以上のパスフレーズ
      ↓
scrypt
  N=32768
  r=8
  p=1
      ↓
32バイト鍵
      ↓
AES-256-GCM
      ↓
.shiro-export
```

saltは16バイト、nonceは12バイトです。

KDF・暗号方式・salt・nonceなどのヘッダーはcanonical JSONへ変換され、AES-GCMの関連データとして暗号化・復号の両方で使用されます。

### 10-2. インポート時の検証

`import_archive()` は、外側のキー集合、format/version、KDF設定、暗号設定、salt/nonce長などを検証します。

AES-GCM復号で `InvalidTag` が発生した場合は、

```text
パスフレーズが違うか、暗号化ファイルが破損しています。
```

という共通エラーに変換します。`InvalidTag` だけから原因を個別に確定する処理はありません。

復号後もschema/version、日時形式、record UUID、セッション形式を確認します。

### 10-3. リソース上限

現行コードには次の上限があります。

| 対象 | 上限 |
|---|---:|
| 暗号化ファイル | 64MB |
| 復号後データ | 128MB |
| セッション数 | 10,000 |
| 全ターン数 | 100,000 |
| 1文字列 | 4,000,000文字 |
| JSON入れ子 | 深さ32 |

この形式はZIP等の圧縮アーカイブではありません。これらは入力サイズ・件数・文字列長・入れ子深さを制限するための値です。

### 10-4. 重複判定

`session_digest()` はセッションをcanonical JSONへ変換し、SHA-256を計算します。インポート時の重複判定はこのダイジェストを使います。

---

## 11. 家計簿・健康記録の連携

この2機能は、**通常チャットとは別に、構造化データをlocalhost上のAPIへ登録する機能**です。どちらも起動直後から自動登録を行うのではなく、ユーザーが該当モードを選択した入力を処理します。

### 11-1. 送信先URLの制限

`is_allowed_local_api_url()` は次を確認します。

```text
scheme == http
URL内の username が無い
URL内の password が無い
host が localhost / 127.0.0.1 / ::1 のいずれか
port が LOCAL_API_PORTS に含まれる
```

`LOCAL_API_PORTS` は、

- `KAKEIBO_BRIDGE_PORT`（既定8767、環境変数で変更可能）
- 8766

から構成されます。

HTTPSを許可していないこと自体が「外部送信防止」を意味するわけではありません。送信先の範囲を決めているのはhost・port等の条件です。

HTTP通信を開く `_open_local_api()` は、

- `ProxyHandler({})`
- リダイレクトを許可しない `_NoRedirectHandler`
- 5秒タイムアウト

を使用します。JSON応答は最大1MBまで読みます。

### 11-2. 家計簿モード

家計簿モードでは `PromptBuilder.build_kakeibo_prompt()` が、LLMへ**1回の入力につき1取引だけ**をJSON候補として出すよう指示します。

ただし、LLMが出したJSONをそのまま登録しません。

#### 金額

最終 `amount` は `kakeibo_amount.extract_amount_result(user_text)` が**ユーザー原文から**決めます。LLM JSON内のamountは最終値として使用しません。

有効候補が複数ある入力は拒否します。不正形式の金額表現が検出された場合も確認画面へ進みません。

#### 日付

最終 `date` もLLM値ではなく `kakeibo_date.extract_date_from_text(user_text)` が決めます。

対応形式は前述の `YYYY/MM/DD`、`YYYY-MM-DD`、`M/D` です。日付が無い場合や有効な対応形式として確定できない場合は実行日を使用します。

#### 店舗・メモ

LLMが候補として出した `store` / `memo` は、空白除去後の文字列がユーザー原文へそのまま含まれる場合だけ候補へ残します。

#### 種別・カテゴリ

`type` は `支出` / `収入` 以外を未確定扱いにします。categoryはtypeごとの定義済み一覧に合わせます。

#### 確認と最終検証

候補は `KakeiboConfirmDialog` で表示し、ユーザーが確認・編集します。登録ボタンの判定とは別に、API送信直前にも `validate_kakeibo_payload()` が日付・金額・type・category等を検証します。

### 11-3. 健康記録モード

健康記録モードでは、LLMへ健康JSONを先頭に出すよう指示します。

対象フィールドは次です。

```text
date
weight
body_fat
muscle_mass
bmr
temperature
pulse
systolic_bp
diastolic_bp
meal_detail
activity_log
memo
```

### 11-4. ユーザー原文を優先する欄

`integrations.extract_explicit_health_fields()` は、ユーザー原文に明示された次のラベルを機械的に探します。

- `食事ログ` / `食事ログ追加`
- `行動ログ` / `行動ログ追加`
- `メモ` / `メモ追加`

見つかった値は同じフィールドのLLM候補より優先してマージします。

### 11-5. Biolog値の検証

数値範囲は次のとおりです。

| 項目 | 範囲 |
|---|---|
| 体温 | 34.0〜42.0（両端を含む） |
| 体重 | 0より大きく300未満 |
| 体脂肪率 | 0〜100（両端を含む） |
| 筋肉量 | 0より大きく200未満 |
| 脈拍 | 30〜200（整数、両端を含む） |
| 収縮期血圧 | 50〜250（整数、両端を含む） |
| 拡張期血圧 | 30〜150（整数、両端を含む） |
| 基礎代謝 | 0より大きく5000未満（整数） |

文字列の `meal_detail` / `activity_log` / `memo` は各2000文字までです。

`bool`、非数値型、有限でない数値、範囲外の数値などは `BiologValidationError` になります。

測定値だけから成る `activity_log` や `memo` を、明示入力でない場合に除去する判定もあります。

### 11-6. Biologの確認

検証後、画面へ登録内容を表示し、`messagebox.askyesno()` で確認します。承認時だけPOSTします。

`user_id` はLLM出力から取得せず、送信時に固定文字列 `"self"` を付けます。

---

## 12. 設定ファイル

`chat_settings.json.example` の公開テンプレートと、`load_settings()` の内部既定値を確認すると、主な設定は次のとおりです。

| キー | テンプレート値 | 意味 |
|---|---|---|
| `model_path` | `C:\path\to\your\model.gguf` | 使用するGGUF。利用者が変更するプレースホルダー |
| `n_ctx` | 8192 | コンテキスト長 |
| `max_tokens` | 1024 | 通常生成の上限 |
| `temperature` | 0.7 | 通常生成へ渡すtemperature |
| `history_retention_days` | 0 | 保存期間。0は無期限 |
| `n_threads_batch` | 12 | llama.cpp性能設定 |
| `n_batch` | 1024 | llama.cpp性能設定 |
| `n_ubatch` | 512 | llama.cpp性能設定 |
| `flash_attn` | true | llama.cpp性能設定 |
| `offload_kqv` | true | llama.cpp性能設定 |
| `use_mmap` | true | llama.cpp性能設定 |
| `tts_enabled` | false | 起動時TTS |
| `tts_rate` | 0 | SAPI5読み上げ速度 |
| `mic_enabled` | false | 起動時音声入力 |
| `whisper_mode` | `auto` | Whisper配置モード |
| `vad_threshold` | 150 | RMS-VADしきい値 |

`chat_settings.json` が無い、またはJSON読み込みに失敗した場合、`load_settings()` は内部既定値を使って処理を続けます。

ただし、すべての設定値を同じ方法で厳密検証しているわけではありません。ロード時に明示的な正規化処理があるのは、性能系整数・bool、TTS速度、VAD値、Whisperモード、保存日数などです。設定画面側にも別の入力検証があります。

設定保存は `atomic_write_json()` を使います。

---

## 13. テストと未確認事項

### 13-1. テスト

`tests/` は標準ライブラリの `unittest` を使っています。現在のテスト結果を確認する場合は、リポジトリルートで次を実行します。

```powershell
python -m unittest discover -s tests -t . -p "test*.py"
```

公開CHANGELOGでは、v1.6.1で全回帰テストが通過し、v1.6.2 / v1.6.3 はコードロジックを変更していないため全回帰テストを再実行していない、と記録されています。

テストには、少なくとも次の領域があります。

- LLMの同時実行・停止・世代管理
- ストリーミングTTS
- 音声ログの本文非出力
- VRAM判定
- DPAPI履歴
- 履歴保存・移行
- 暗号化エクスポート/インポート
- 家計簿の金額・日付・確認
- Biologサニタイズ
- ローカルAPI送信先判定
- JSON抽出

### 13-2. テストがあることと実機確認は別

自動テストではモックや疑似オブジェクトを使う領域があります。そのため、テスト通過だけから次を保証することはできません。

- 利用者のGPUで任意のGGUFが読み込めること
- 実際のマイク・オーディオドライバで音声認識できること
- 利用環境のSAPI5音声が期待どおり再生されること
- 別プロジェクトの家計簿/Biologサービスが正しく構成されていること
- すべてのGPU/CPU/ドライバ構成で同じ性能になること

### 13-3. モデルファイルの完全性

本アプリは会話履歴キャッシュや持ち出し履歴の重複判定にSHA-256を使用しますが、**GGUFモデル本体について期待SHA-256を照合するコードはありません**。

モデル読み込み時に例外になる破損もありますが、あらゆる取り違え・改変・破損をアプリが検出する仕組みではありません。配布元がチェックサムを提示している場合は、利用者側での照合が別途可能です。

### 13-4. 標準出力ログ

アプリは各所で標準出力へ診断ログを出します。専用ログファイルへ自動保存する実装は確認できません。

`test_audio_log_privacy.py` はTTS本文とWhisper認識本文がそれぞれの診断ログへ直接含まれないことを確認しています。一方、これはアプリ内のあらゆるログ出力について機密情報非出力を保証するテストではありません。

---

## 14. 用語集

| 用語 | この文書での意味 |
|---|---|
| **LLM** | このアプリでは、`llama-cpp-python` から読み込んで対話生成に使う言語モデル |
| **GGUF** | llama.cpp系で読み込むモデルファイル形式 |
| **llama-cpp-python** | Pythonからllama.cppを利用するためのライブラリ |
| **コンテキスト長 (`n_ctx`)** | 1回の生成でモデルへ渡せる文脈量の設定 |
| **トークン** | モデルのtokenizerが文章を分割して扱う単位 |
| **temperature** | LLM呼び出しへ渡す生成パラメータの1つ |
| **ストリーミング** | 生成完了まで待たず、届いた文字列断片を順次処理する方式 |
| **VRAM** | GPUが利用するメモリ |
| **OOM** | Out Of Memory。メモリ不足を表す状態 |
| **VAD** | Voice Activity Detection。ここではRMSを使った発話区間判定 |
| **RMS** | 音声振幅から計算する値。VADしきい値判定に使用 |
| **Whisper** | 音声認識モデル。現行コードでは `whisper` パッケージを使用 |
| **TTS** | Text To Speech。テキスト読み上げ |
| **SAPI5** | 現行Windows TTSで利用している音声API |
| **Tkinter** | PythonのGUIインターフェース |
| **スレッド** | 同一プロセス内で別の処理を並行して進める実行単位 |
| **デーモンスレッド** | Pythonプロセス終了時の待機対象にならないよう `daemon=True` で作られたスレッド |
| **キュー** | 順番待ち用のデータ構造。TTSで `queue.Queue` を使用 |
| **LRU** | 最近使われていない要素を先に外すキャッシュ方針。`TokenCostCache` で使用 |
| **ヒステリシス** | 切替開始と復帰に異なるしきい値を持たせる方式 |
| **DPAPI** | Windows Data Protection API。通常履歴の保護に使用 |
| **AES-GCM** | 持ち出し履歴の暗号化と認証に使用 |
| **scrypt** | パスフレーズから暗号鍵を導出するKDF |
| **SHA-256** | 履歴キャッシュ署名や重複判定で使用するハッシュ関数 |
| **サニタイズ / 検証** | APIへ送る前にキー、型、範囲などを制限する処理 |
| **localhost / loopback** | 同じPC自身を指すネットワーク宛先 |

---

## 最後に

LLM Local Chat v1.6.3 の現行実装は、単一のGUIファイルだけで処理を完結させず、LLM実行、プロンプト構築、VRAM判定、履歴保護、音声処理、ローカルAPI連携を別モジュールへ分けています。

特にコード上で確認できる特徴は、次のとおりです。

- LLM処理は同時開始を拒否し、待ち行列化しない
- LLMロードはGPU/CPUと引数構成を変えながらフォールバックを試す
- 推論前にVRAMを再取得し、GPU推論時は生成上限を調整する
- WhisperはCPU `small` を基礎に、条件を満たす場合はGPUモデルも利用する
- TTSとマイクの相互干渉を世代番号と抑制フラグで扱う
- 通常履歴はDPAPIで保存し、保存前に暗号化結果を復号照合する
- 旧平文履歴の移行は、置換前後に複数の確認を行う
- 持ち出し履歴はDPAPIではなくパスフレーズ + scrypt + AES-256-GCMを使う
- 家計簿の金額と日付はLLM値を最終値にせず、ユーザー原文から決定する
- 健康記録はキー・型・範囲を検証し、明示ラベルの原文を優先する
- 連携APIの送信先はloopbackホストと許可ポートへ制限する

これらは**現在のコードに実装されている処理の説明**であり、「あらゆる環境で障害が起きない」「セキュリティ上の問題が存在しない」といった保証を意味しません。実運用では、利用するGGUF、Windowsユーザー環境、Python依存関係、GPU/音声デバイス、連携先サービスの構成も合わせて確認する必要があります。

---

*この版は、v1.6.3 の `LLM_Local_Chat.py` と関連モジュール、`chat_settings.json.example`、起動バッチ、回帰テスト、README / CHANGELOG を再確認して作成しました。*

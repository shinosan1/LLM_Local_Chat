# Security Policy

## サポート対象

セキュリティ対応の対象は、リポジトリで公開している最新版のみです。過去のバージョンはサポートしません。

## 脆弱性の報告

脆弱性の詳細、再現手順、攻撃方法などを公開Issueへ投稿しないでください。次のメールアドレスへ非公開で報告してください。

`shino.project.support@gmail.com`

報告時は、影響を受けるバージョン、問題の概要、再現に必要な最小限の情報、想定される影響を記載してください。認証情報、個人情報、実データなどの機密情報は送信しないでください。

本プロジェクトは個人開発です。報告への回答、調査開始、修正の実施および公開までの期限は保証しません。

## 添付ファイルと外部prompt

### 保持範囲と削除範囲

通常チャットの添付は、添付時点の内容を`chat_logs/attachments/<session-id>/`以下へコピーし、検証済みのセッションIDと添付内部IDでチャットへ関連付けます。元ファイル名や元ファイル絶対パスを保存・削除パスとして使用しません。新規チャット、別チャット読込、モデル切替、アプリ終了は削除条件ではなく、保存済みチャットの再読込・再起動後に復元します。元ファイルの編集・移動・削除は保存済みコピーへ反映されません。

履歴JSONには内部ID、basename、種別、MIME、拡張子、サイズ、SHA-256だけを保存し、テキスト本文、画像bytes、base64/data URI、元ファイル絶対パスは直接保存しません。添付実体はDPAPI履歴JSONとは別のsidecarであり、アプリ自身による暗号化保存は行いません。`.shiro-export`には添付実体も添付メタデータも含めず、インポート後に復元しません。ただし、LLMが添付内容や名前を回答へ引用した場合、そのAI回答は通常履歴としてDPAPI保存され、要約へ含まれる可能性があります。メモリや画面、標準出力、OSのページファイルをzeroizeする機能もありません。

入力欄の「添付解除」は確認後、現在チャットの全添付について、チャットとの関連メタデータと対応sidecarを削除します。チャット削除はそのチャット専用の添付を削除します。設定画面の個別削除は選択した内部IDだけ、一括削除は現在のアプリ保存領域で管理される全チャットの保存済み添付だけを対象とし、会話本文・AI回答は残します。削除に失敗した実体はメタデータだけを消して未管理の孤児にせず、保留名で再試行可能な状態を維持します。未知ファイル、reparse point、I/Oエラー等の部分失敗は成功・失敗・スキップ・再試行待ち件数として表示します。ごみ箱、復元、自動バックアップ、自動期限削除はありません。

### ローカル実行とマルチユーザー

添付機能自体はネットワーク送信を行わず、画像はVision推論時だけローカルdata URIとしてローカルhandlerへ渡します。しかし「ローカル保存・ローカル推論」は完全な安全を意味しません。本アプリはユーザーアカウント単位の添付保存領域分離、認証、権限管理、暗号化を提供しません。同じWindowsアカウント、同一PC、共有フォルダ、Windowsの別ユーザー等で同じアプリデータ保存領域を複数人が利用する場合、一括削除は他の利用者を含む全チャット添付へ影響し得ます。PCへの不正アクセス、マルウェア、デバッガ、保存フォルダへアクセスできるプロセス、OSやディスク側の保護範囲は本アプリの保証外です。

個人情報、業務機密、パスワード、APIキー、アクセストークン、秘密鍵等を含むファイルは、端末・Windowsアカウント・実行プロセスを信頼できる場合に限って扱ってください。OSのアクセス制御・BitLocker等はアプリ自身の保証とは別です。保存済み添付はディスク容量を累積使用しますが、自動容量制限・自動消去・バックアップを提供しません。重要な元ファイルの代替にしないでください。

### 非実行とprompt injection

TXT / MD / JSON / CSVにコマンド、Python、PowerShell、shell、JavaScript等が含まれていても、自動実行や`subprocess`への受け渡しは行わず、LLMへの入力データとして扱います。ただし、添付文書内の命令文を信頼済み命令と識別できる完全な防御はなく、prompt injectionにより回答が誘導される可能性があります。外部文書の記述や生成結果を検証せず、ファイル削除、コード実行、API送信その他の重要操作へ利用しないでください。

### ファイル・パス・形式の検査

対応形式はTXT / MD / JSON / CSV、PNG / JPG / JPEGです。テキストはUTF-8、1ファイル1MiB以下、画像は10MiB・16メガピクセル以下で、PillowがPNG/JPEGとして検証できるものに限定します。最大8件、画像1枚までです。これらは異常入力の影響を抑える境界であり、マルウェア検査、内容の正当性、promptの安全性を保証しません。

ファイルダイアログで利用者が選択したパスは添付時の読み取りにだけ使用し、LLMへは制御文字とパス区切りを除いたbasenameだけを渡します。sidecar名と削除キーは検証済みUUIDから構築し、`../`、絶対パス、特殊な元ファイル名による管理領域外の書込み・削除を拒否します。管理領域内でもreparse pointは追跡せず、未知ファイルは自動削除しません。個別削除はファイル名ではなく内部IDを使用するため、同名の別添付を削除対象にしません。

保存済みsidecarを外部操作で削除・移動した場合、読取不能になった場合、保存サイズまたはSHA-256がメタデータと一致しない場合は、その添付をLLMへ渡さず日本語で警告します。存在しない内容を推測させる自動代替や大規模な自己修復は行いません。形式・UTF-8・サイズ・画像画素数・SHA-256検査は異常入力や偶発改変の影響を抑えるもので、マルウェア検査やファイルの安全性を完全保証するものではありません。

添付を後続ターンへ再利用するとcontextとメモリを継続消費します。添付がある場合は全履歴を含めた既存token計算で上限超過を事前拒否し、画像は4,096 tokensを予約します。Vision handlerがモデル固有の正確な画像token数を事前公開しない場合があるため、実行時のcontext例外も捕捉してユーザーへ表示します。

`chat_settings.json`で指定する外部promptは、利用者が信頼できるUTF-8のTXT / MDだけを指定してください。相対パスはアプリ配置フォルダ基準で読み取り、内容をsystem promptへ組み込みますが、自動実行は行いません。

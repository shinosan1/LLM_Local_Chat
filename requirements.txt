# LLM Local Chat - 依存ライブラリ
#
# ※ PyTorchはCUDA 12.4版を固定しています。
#   CPU環境ではこの3行と追加インデックスを環境に合わせて変更してください。
#
# ※ llama-cpp-python はWindows CUDA 12.4版wheelを直接指定しています。
#   CPU版へ差し替えないでください。CUDA以外の環境ではこの行を環境に合わせて変更してください。
#
# ※ PyAudioのインストールでエラーが出る場合：
# PyAudioが導入できない場合はPython/PyAudioの対応版と公式wheelを確認すること。

# --- 既存のライブラリ ---
--extra-index-url https://download.pytorch.org/whl/cu124

llama-cpp-python @ https://github.com/abetlen/llama-cpp-python/releases/download/v0.3.34-cu124/llama_cpp_python-0.3.34-py3-none-win_amd64.whl
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
Pillow==12.2.0
PyAudio==0.2.14
numpy==2.4.3
openai-whisper==20250625

# --- 今回のSAPI5制御(停止・スレッド対応)で追加 ---
pywin32==309

# --- VRAM安全フィルタ (v1.2.0) で追加 ---
# GPU memory monitoring (required for VRAM safety filter)
# pynvml が未インストールの場合、nvidia-smi コマンド経由でフォールバックする。
# GPU なし環境では両方不要（CPU-only モードで自動動作）。
nvidia-ml-py==13.610.43
# Official PyPI package (Python Cryptographic Authority)
cryptography==49.0.0

# CPU/GPU load metrics (non-decision metric — ログ・診断専用)
psutil>=5.9.0

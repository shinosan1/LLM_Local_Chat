# Changelog

本ファイルはLLM Local Chatの変更履歴を記録します。  
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) を参考にしています。過去版には保存形式の互換性変更をパッチ版として公開した例があります。

---
## [Unreleased]

---
## [1.8.1] - 2026-08-28

### 変更
- 公開用の起動バッチを `LLM_Local_chat_run.bat`、コード解説を `code_reference.md`、SHA-256一覧を `SHA256.md` へ改名し、3拠点の関連参照を更新
- `TERMS_OF_USE.md`、`PRIVACY_POLICY.md`、`DISCLAIMER.md`、`THIRD_PARTY_LICENSES.md` を追加し、READMEから参照可能にした
- `APP_VERSION` とREADMEの現在版表示をv1.8.1へ更新。チャット処理、保存形式、添付管理、ローカルAPI連携、UIの仕様は変更していない
- 公開用ルートの実ファイルを基準に `SHA256.md` を更新し、一覧自身を除く公開対象の実測値を記録した
- GitHub公開用のモデル配置プレースホルダを `models/ここにモデルを追加してください.txt` から `models/PLACE_MODELS_HERE.txt` へ改名

---
## [1.8.0] - 2026-08-27

### 追加・変更
- TXT / MD / JSON / CSVおよびPNG / JPEGを、内部IDとセッションIDでチャットへ紐づけ、`chat_logs/attachments/`へ添付時点のコピーとして永続保存。チャット切替・新規チャット・終了では削除せず、保存済みチャットの再読込・再起動後に復元する
- テキスト添付は後続ターンごとに履歴とは分離して1回だけ再投入し、画像はVision利用時だけdata URIへ変換。本文なし＋添付ありの送信と、同名・別内容ファイルの添付に対応
- 設定画面へ「保存済み添付ファイル」を追加し、ファイル名・種類・サイズ・紐づくチャット・状態、総件数・合計容量、内部ID単位の個別削除、全チャット対象の一括削除を提供
- `system_prompt`、`user_personalization`、外部prompt、応答言語、思考非表示指示を排他にせず順序どおり結合し、同一内容だけを重複排除

### 修正
- 保存済み添付の一覧・個別削除・一括削除で、各履歴の読込ごとに全namespace recoveryと履歴復号を繰り返していた処理を1操作1回のスナップショットへ集約。変更のない履歴ファイルのSHA-256再計算も省略
- 添付管理の一覧取得・整合性検証・削除・削除後スナップショット構築を単一workerへ移し、Tkメインスレッドの長時間停止と重複操作を防止。生成・モデル再配置・portable処理・終了処理との競合も拒否
- 一括削除をセッション単位でステージ・履歴更新するようにし、同一セッション内の途中失敗ではロールバックし、物理削除未完了はpending-deleteとして再試行可能な状態を維持
- 保存済み添付管理のTreeview・見出し・選択行・スクロールバーへダイアログ専用ダークStyleを適用し、生成設定の固定幅依存を減らしてWhisper実行モード等の日本語表示切れを修正

### 安全性・互換性
- 履歴JSONには添付ID・basename・種類・サイズ・SHA-256等のメタデータだけを保存し、本文、画像bytes、base64/data URI、元ファイル絶対パスは保存しない。旧履歴・旧`chat_settings.json`は変換不要
- 復元時に実体欠損、読込不能、サイズ不一致、SHA-256不一致を検出した添付はLLMへ渡さず警告。保存・削除パスは検証済みUUIDから生成し、reparse pointと管理領域外パスを拒否
- 一括削除は会話履歴を残し、成功・失敗・スキップ・再試行待ち件数を表示。ユーザー単位の保存領域分離はなく、共有保存領域では他利用者を含む全チャット添付へ影響し得ることをUIと文書へ明記
- `.shiro-export`からローカル添付メタデータと添付実体を除外し、インポート後に添付が復元されないことを明記

### テスト
- 再起動復元、元ファイル独立、同名別ID、個別・一括・チャット削除、欠損・改変、path traversal/reparse、添付のみ送信、portable除外、inline＋external結合、context超過、Vision拒否を追加・更新。既存回帰を含む全640件を実行し、639件成功・1件skip（symlink作成権限なし）
- 添付管理のrecovery呼出回数、履歴SHA-256再利用、同一セッション内rollback、重複session ID拒否、worker多重操作・終了競合・stale結果破棄、専用Style、Whisper全文表示、実Tk削除smokeを追加。全666件を実行し、665件成功・1件skip（symlink作成権限なし）

---
## [1.7.3] - 2026-08-26

### 追加
- `chat_settings.json`へsystem prompt、persona、応答言語、内部思考を表示しない指示、UTF-8の外部TXT / MD prompt読込を追加。既存設定ファイルは変換不要で、外部ファイルの欠損・読込失敗時も警告して起動を継続
- 通常チャットへTXT / MD / JSON / CSV、PNG / JPEGの添付UIを追加。添付は送信1回限りで、実パス・画像バイナリ・テキスト本文を履歴JSONへ保存しない
- Visionモデル設定として`vision_enabled`、`vision_handler`、`vision_projector_path`を追加。非対応モデルでは画像送信を拒否し、対応時はllama-cpp-python 0.3.34のMTMD chat handlerへローカルdata URIを渡す

### 修正
- Windows日本語IMEの未確定文字だけ別フォント・別サイズで表示される問題を修正。チャット入力欄の実フォントをIME composition fontへ同期し、通常文字のフォントサイズは維持

### 安全性・互換性
- テキスト1MiB、画像10MiB・16メガピクセル、添付8件・画像1枚の上限を設け、超過時は切り捨てずUIへ表示
- 画像用に4,096トークンをMTMD上限とcontext事前予約へ反映し、Vision handlerをLLMより先に解放。テキスト専用モデルの既存ロード引数と既存履歴形式を維持

### テスト
- 旧設定互換、外部promptの優先・重複排除・読込失敗、対応添付形式・サイズ、添付の一回限り利用と履歴非永続、Vision拒否・MTMD handler lifecycle・context上限の回帰テストを追加

---
## [1.7.2] - 2026-08-25

v1.7.1 に対する後方互換の不具合修正版。

### 修正
- 日付を省略したBiolog登録の成功時に、完了表示が `?` になる問題を修正
- 送信直前にJSTの登録日を確定してPOST recordへ保持し、API応答に有効な日付が
  ない場合は、実際にPOSTした確定日付を完了表示へ使用するようにした
- 明示日付は変更せず、API失敗時に成功表示を出さない既存挙動を維持

---
## [1.7.1] - 2026-08-24

v1.7.0 に対する後方互換の不具合修正版。

### 修正
- Full GPUの既存安全条件を満たさない場合、GGUF metadataの総レイヤー数と
  利用可能VRAMから約75%・50%・25%のpartial GPU offloadを段階選択し、
  すぐCPUへフォールバックする性能の崖を緩和した。Full条件と予約量は緩和していない
- GPUメモリ由来と明示できるロード失敗、またはロード後の最低予約量不足では、
  モデルを安全に解放して一段低いoffload候補を試す。非VRAMエラーは層数変更で隠さない
- 生成設定でLLM GPUオフロードの希望モード（自動・Full GPU・約75%・約50%・約25%・
  CPU）を選択し、アプリを再起動せずhot reloadできるようにした。永続化するのは希望モードだけ
- 実際にロード成功したGPUオフロード率・配置レイヤー数・実行方式は希望設定と分けて
  読み取り専用表示する。actual runtime stateは設定ファイルへ保存しない
- `自動`モードの推論直前に最低VRAM予約量を下回った場合、旧LLMを先に解放して
  現在より低いoffloadへ再配置し、同じ入力を最大1回だけ継続するようにした。
  UI表示・履歴・TTS・家計簿確認/POSTは安全確認後に一度だけ開始する
- LLM hot reload中は新規操作と内部Whisper転写を止め、進行中転写の終了を有限時間待つ。
  reload世代が古い結果や終了開始後の結果はattachせず、安全に解放する
- 家計簿モードの日付機械抽出に `M月D日` と `YYYY年M月D日` を追加した。
  既存の数字正規化を使うため、全角数字の同形式にも対応する
- 無効な年付き日本語日付を、内部の年なし月日として実行年で再解釈しないようにした。
  例として、実行年が閏年でも `2025年2月29日` を `2024-02-29` として採用しない
- `8/20 1603円` の日付末尾と正常金額を、数字列途中に空白がある不正金額として
  誤検出していた問題を修正した。`1 603円` などの不正表記は引き続き拒否する
- `2170円8月18日` のように金額直後へ `M月D日` / `YYYY年M月D日` が続く入力を、
  後続数字の不正連結として除外していた問題を修正した。実在しない暦日や通常の数字連結は
  金額候補として許可しない
- `YYYY/MM/DD`、`YYYY-MM-DD`、`M/D`、日付なし時の実行日フォールバック、
  LLM生成 `date` を最終値にしない仕様は変更していない
- 複数取引の `source_text` は完全一致を優先し、LLMによる半角/全角スペースだけの
  削除・追加・置換がある場合は、一意で非重複な原文spanへ対応できるときだけ
  元の原文スライスへ復元するようにした。句読点・数字・改行等の差や曖昧一致は拒否する
- 家計簿プロンプトで `source_text` の文字単位完全一致と、JSON前後へ説明文を
  付けないことを明示した

### テスト
- Full・約75%・50%・25%・CPUの選択境界、総レイヤー数の動的計算、metadata取得失敗、
  GPUメモリ不足時の段階降格、ロード後安全確認、非VRAMエラー、再ロード世代、
  GPUオフロード表示、手動hot reload、Whisper排他、終了競合の回帰テストを追加
- 推論直前のhard limitからAuto downshiftして同一入力を1回だけ継続する経路を、
  通常チャット・家計簿・健康記録で固定し、表示・履歴・TTS・家計簿確認の重複がないことを確認
- 日本語の年付き・年なし・全角形式、無効日、既存形式、複数取引の日付一意性、
  確認候補から最終送信payloadまでの回帰テストを追加
- 日付直後の正常金額、日付付きの不正な空白入り金額、複数金額、実際の自然入力を
  金額単体から確認候補・最終送信payloadまで通す回帰テストを追加
- 実測した5件入力、半角/全角スペース差6種、非空白差・曖昧一致・重複spanの拒否、
  5件の逐次確認・callback待ち・逐次POST、家計簿プロンプト契約の回帰テストを追加
- 金額直後の日本語日付、全角、無効日、通常の数字連結、実測5件入力の金額span・
  候補生成・callback待ち・逐次POSTを固定する回帰テストを追加

### 文書
- README・CURRENT_SPEC・コード解説で、LLM Local Chatが家計簿候補生成・確認・
  `kakeibo-bridge` APIへのPOSTクライアントを持つ一方、家計簿アプリ、DB、
  bridgeサーバーおよびそのDocker/Compose定義は本リポジトリに含まれないことを明確化
- Git上では2026-07-11のcommit `1e448f0`で家計簿モードUI・JSON抽出・確認・HTTP POSTを
  確認できる。ただし当時の版数表示と履歴に不整合・欠落があるため、それ以前の正確な導入版は断定しない

---
## [1.7.0] - 2026-08-18

家計簿モードで1回の入力から複数の取引を扱えるようにした機能追加版。
**一括登録ではなく「1取引 = 1確認 = 1 POST」の安全境界は維持している。**

### 追加
- 家計簿モードで、**1回の入力から最大10件の取引候補を生成**できるようにした
  (`MAX_KAKEIBO_TRANSACTIONS_PER_INPUT`)。
  **一括登録ではなく「1取引 = 1確認 = 1 POST」の安全境界は維持する。**
  変更したのは入口(1入力から複数候補を作れること)だけで、確認と送信は従来どおり
  1件ずつ行う
- 分割・検証を担う `kakeibo_split.py` を追加。LLMが返す取引ごとの原文断片
  (`source_text`)を信用せず、次を検証する
  - 空文字ではない
  - 元のユーザー入力に実在する文字列である
  - 原文上で範囲が重複していない
  - 原文順に並べられる(並びが逆でも、各断片が原文中にちょうど1回だけ現れる場合は
    出現位置で並べ直す。一意に決められない場合は推測せず拒否)
  - `source_text` / `store` / `category` / `type` / `memo` 以外のキーを含まない
- `json_extractors.extract_kakeibo_transactions()` を追加。LLM応答から
  `{"transactions": [...]}` を取り出す。旧形式(単一レコードJSON)しか返らなかった
  場合は、入力全体を1取引として扱う従来経路へフォールバックする
- `kakeibo_date.find_explicit_date()` を追加。原文に明示的な日付表現がある場合だけ
  値を返し、実行日へのフォールバックを行わない。`extract_date_from_text()` の
  公開挙動は変更していない
- `IntegrationBridge.send_kakeibo()` に完了通知(`on_complete`)を追加。
  POSTの成否をUIスレッドから1回だけ返す

### 変更
- `PromptBuilder.build_kakeibo_prompt()` の雛形を `transactions` 配列形式へ変更。
  **`amount` と `date` は雛形に含めない**(最終値をLLMに決めさせないため)
- `ChatApp._confirm_and_send_kakeibo()` の引数を候補リスト1つへ変更し、
  逐次確認・逐次POSTの制御を追加。前のPOSTの完了通知を受けてから次の確認画面を
  開くため、**複数POSTは並列化されない**
- `KakeiboConfirmDialog` を最小拡張。複数候補のときだけタイトルへ `(2 / 3)` を表示し、
  「この取引をスキップ」と「残りを中止」を分けた。ウィンドウを閉じる操作は
  「残りを中止」と同じ扱いにする。1件だけのときは従来どおりの2ボタン構成

### 安全性
- 取引が11件以上の場合は**入力全体を拒否**する(確認画面0件・POST 0件)。
  先頭10件だけ処理する部分成功は行わない
- 1つの取引断片に有効な金額候補が複数ある場合、1取引なのか内訳なのかを推測せず
  入力全体を拒否する
- 最終 `amount` と `date` は従来どおりLLM値を採用せず、各取引の原文断片から
  `kakeibo_amount` / `kakeibo_date` が機械抽出する。断片自身に日付表現が無い場合だけ
  入力全体の日付表現へフォールバックする(先頭の日付が全取引へかかる書き方を
  壊さないため)。対応する日付形式は変更していない
- `store` / `memo` は、その取引の原文断片へそのまま含まれる場合だけ候補へ残す
- 途中のPOSTが失敗した場合は、成功したかどうかが不明な状態で次を送らないよう
  残りの候補を処理せず停止する。すでに登録済みの取引は取り消さない
- 前の登録シーケンスが進行中の間は、新しい入力から2つ目のシーケンスを開始しない

### 安全性（追加レビューによる修正）
- **LLMが取引を取りこぼした場合の部分登録を防止した。** 原文の金額表現をすべての
  取引断片が覆っているかを確認し、どの断片にも含まれない金額が残っている場合は
  入力全体を拒否する。件数上限(10件)の判定だけでは、原文に11取引あるのにLLMが
  10件しか返さないケースを検出できなかった。判定には既存の金額抽出ロジックを
  再利用する `kakeibo_amount.find_amount_spans()` を使い、日付・電話番号・数量を
  金額と誤認しないようにしている
- **同一文脈内の複数金額を人工的に分割した候補を拒否するようにした。**
  「スーパーで2000円と500円」をLLMが「スーパーで2000円」「500円」へ切り分けた場合、
  各断片に金額が1個ずつあるため従来は2件として通っていた。金額表現を取り除いた残りに
  実質的な内容が無い断片(金額だけの断片)は、独立した取引として受理しない
- **日付フォールバックの曖昧性を解消した。** 断片に日付が無い場合のフォールバックを、
  原文全体の明示日付が**ちょうど1種類のときだけ**に限定した。
  「8/16 スーパーで2000円、8/17 コンビニで500円、薬局で1200円」では薬局の日付を
  一意に決められないため、最初に見つかった日付を流用せず入力全体を拒否する
- **家計簿の確認・POSTシーケンス中は新しい送信を開始しないようにした。**
  `Controller.is_busy()` が `_kakeibo_sequence_active` も見るようにしたため、
  キーボード入力・音声入力とも既存の busy 判定で止まる。従来は前のPOST完了を待つ間に
  操作状態が idle へ戻っており、LLM処理を最後まで走らせてから「登録処理中」と
  断るUXになっていた
- **予期しない例外でシーケンス進行中フラグが残らないようにした。** 確認ダイアログの
  生成・送信開始・完了通知の各区間を例外ガードで囲み、いずれで失敗しても
  `_kakeibo_sequence_active` を解除する。送信開始に成功した場合は完了通知側が
  続きを進めるため、非同期POST中に早期解除しない構造を保っている

### 追加（安全検証用）
- `kakeibo_amount.find_amount_spans()`: 有効な金額表現を原文上の位置付きで返す。
  `extract_amount_result()` と同じパターン・換算規則を使い、判定の二重実装を避ける
- `kakeibo_date.find_explicit_dates()`: 原文中の明示日付をすべて出現順に返す。
  対応形式は増やしておらず、`extract_date_from_text()` の公開挙動も変更していない

### テスト
- `tests/test_kakeibo_split.py` を追加(31件)。3件分割、上限10件受理、11件で全体拒否、
  source_text捏造・重複・逆順、amount/date捏造、断片内複数金額に加え、
  LLMの取引取りこぼし・金額だけの断片・複数日付での曖昧なフォールバックを
  それぞれ全体拒否することを検証
- `tests/test_kakeibo_sequence.py` を追加(11件)。**本番の
  `ChatApp._confirm_and_send_kakeibo()` を直接呼ぶ**方式へ変更し、制御フローを
  テスト側で再実装しないようにした。確認ダイアログ・`wait_window`・送信を差し替え、
  順序、並列化されないこと、スキップ後の続行、中止・×ボタンで残りを開かないこと、
  POST失敗で停止すること、例外時にフラグが残らないことを検証
- `tests/test_json_extractors.py` に `extract_kakeibo_transactions` のテストを追加(5件)。
  雛形が `amount` / `date` を含まないことの検証も追加
- `tests/test_llm_concurrency.py` の家計簿コントローラテストを新シグネチャへ更新。
  仕様変更に伴う正当な期待値変更であり、失敗を隠すための書き換えではない
- `tests/test_llm_concurrency.py` に busy 判定のテストを追加(4件)
- 全回帰テスト472件が成功(変更前402件 + 新規70件)

### 修正（追加レビューによる）
- **家計簿シーケンス中のUI表示を内部状態と一致させた。** `_finish_operation()` が
  無条件に送信ボタンを `normal` へ戻していたため、POST完了待ちの間は
  `Controller.is_busy()` が真なのにUIだけ操作可能に見える経路があった。
  シーケンス進行中は送信ボタンを戻さず、`operation_label()` も
  「🏠 家計簿確認・登録中…」を返すようにした。再有効化はシーケンスの終了処理が
  担当し、他のLLM処理が走っていない場合だけ戻す。全終了経路(全件成功・全件スキップ・
  残りを中止・×で中止・POST失敗・確認ダイアログ例外・send_kakeibo例外)で復旧する
- **1件入力の確認画面に入力全文を表示するようにした。** LLMが1件入力に対して
  入力の一部分だけを `source_text` として返した場合、確認画面の「認識・入力内容」に
  部分文字列だけが表示されていた。1件のときは確認画面用の `source_text` も
  入力全文にする(amount/date の抽出元が入力全文である既存挙動は変更していない)

### テスト（追加レビューによる）
- `tests/test_kakeibo_sequence.py` に**非同期の完了待ちを直接検証するテスト**を追加。
  完了通知を自動で呼ばない deferred callback 方式のフェイクを用意し、
  callback を呼ぶまで次の確認ダイアログもPOSTも開始しないこと、callback 後にだけ
  次へ進むこと、失敗 callback で残りを停止することを段階的に確認する。
  従来の同期型テストも残している
- 送信ボタンの状態遷移テストを追加(POST待ち中は disabled のまま、全終了経路で
  normal へ復旧、別のLLM処理が続く場合は戻さない)
- `operation_label()` と `_finish_operation()` のテストを追加
- 1件入力で `source_text` が入力全文になること、amount/date の抽出元が
  変わっていないことのテストを追加
- 全回帰テスト472件が成功

### 備考
- 音声入力専用の経路は作っていない。Whisper認識結果も従来どおり通常のテキスト処理へ
  入るため、キーボード入力と音声入力で同じ複数取引処理を使う
- 日付解析の対応範囲は拡張していない。相対日付表現や自然言語日付の解析は
  今回も追加していない

---
## [1.6.3] - 2026-08-18

### 変更
- `コード解説_やさしい版.md` を全面改稿。段階的な訂正を重ねた結果、記述の根拠と章構成を
  統一する必要が生じたため、**現行コードを最終根拠として全文を再構成**した。
  根拠の優先順位（1. Pythonコード・設定テンプレート・起動バッチ、2. 回帰テスト、
  3. README/CHANGELOGなどの公開文書）を文書冒頭に明示し、コードと文書が食い違う場合は
  現行コードを優先する方針とした。行番号・総行数・テスト件数など更新で変わりやすい値は
  本文へ固定しない方針も明記した

### 修正
- 全面改稿にあたり、旧版に残っていた**実装との不一致3件**を新たに検出し修正した
  - `kakeibo_date.py` を「『おととい』『先週の金曜』などを日付に直す」と説明していたが、
    **自然言語の相対日付を解析するコードは存在しない。** 対応形式は `YYYY/MM/DD`、
    `YYYY-MM-DD`、`M/D` のみで、月/日形式は実行年を補い、未対応形式は実行日へ
    フォールバックする
  - `kakeibo_amount.py` を「『1万5千円』『¥1,500』などを数値に直す」と説明していたが、
    **複合単位表記は `invalid_amount_format` として拒否される。** 対応単位は
    `円` / `千円` / `万円` で、小数・符号付き・指数表記・数字途中の空白も不正形式として扱う。
    有効候補が複数ある場合は `multiple_amounts` となり登録へ進まない
  - データ保存先の一覧に「テキスト書き出し / 平文 .txt」を記載していたが、
    **現行の「ファイル」メニューに平文保存の項目は存在しない。** v1.5.0で
    パスフレーズ付き暗号化エクスポートへ置き換え済み
- 次の実装事実を新たに記載した
  - 家計簿モードのやり取りは通常会話履歴へ追加せず、要約の対象にも入らない
  - 家計簿の `amount` と `date` はLLM出力を最終値にせず、ユーザー原文から機械抽出する。
    `store` / `memo` はユーザー原文に含まれる場合だけ候補へ残す
  - `resource_monitor.py` の `VRAMGuard` は定義のみで、現行の他モジュールから参照されていない
  - `atomic_io.py` はファイルディスクリプタを `fsync` するが、置換後のディレクトリの
    `fsync` は行わないため、電源断時の永続性まで保証する表現は使用しない
  - `ChatApp._save_now()` はDPAPI保存に失敗しても平文へ自動フォールバックしない
- 外部通信に関する説明を正確化。「外部へ出ていく通信は2つだけ」「唯一データが外に
  出ていく経路」といった断定をやめ、推論・音声認識・音声合成がローカル処理であること、
  連携データはlocalhostのAPIへ送信すること、Whisperモデルの初回ダウンロードのみが
  インターネット通信であることを機能ごとに記述
- localhost制限とHTTP制限の役割を明確化。「httpsを許すと外部サーバへ送れてしまう」は
  誤りで、scheme（http/https）と送信先（localhost/外部）はURLの別要素である。
  外部送信を防いでいるのはホストとポートの許可リスト、リダイレクト禁止、
  プロキシ無効化であり、HTTPへの限定は本実装の方針である旨へ訂正
- `os.replace()` の原子性に関する説明を訂正。「別ドライブだとコピー＋削除に落ちる」は
  誤りで、異なるファイルシステム間では失敗する場合がある。コピー＋削除への自動
  フォールバックは行われない
- DPAPIの保護範囲をより正確に記述。「別ユーザーは読めない」「二度と読めなくなる」を、
  「通常は復号できない」「必要なユーザープロファイルや資格情報を失うと復号できなく
  なる可能性がある」へ変更
- マイク監視が音声入力ON時のみであることを明記。「マイクを常時読み続けます」という
  記述が、既定OFFであるという冒頭の説明と矛盾していた
- LLMの排他制御を「順番待ち」ではなく「同時開始拒否」と訂正。`_start()` は実行中なら
  `return False` を返して開始要求を拒否しており、待ち行列に積む実装ではない
  （キューを持つのは `TTSWorker` 側）
- 公開文書に残っていた内部 `docs/` パス2箇所を修正（公開リポジトリではルート直下）
- AES-GCMの説明を訂正。「改ざんされていないことを保証」から「認証タグにより改ざんを
  検出する」へ変更し、`InvalidTag` が鍵・nonce・関連データの不一致でも発生することを明記
- SAPI5の説明を訂正。SAPI5自体はWindowsに含まれるが、利用できる日本語音声は
  Windows環境に依存し、音声の追加インストールが必要な場合がある
- 「行数を本文に書かない」方針に反して残っていた `audio_workers.py` の行数表記を削除
- 連携APIの許可ポートの説明を実装どおりに訂正。「ポート 8767・8766 のみ」という固定値の
  断定をやめ、`LOCAL_API_PORTS` が `KAKEIBO_BRIDGE_PORT`（環境変数で変更可、既定8767）と
  `8766` で構成される旨へ変更。コード例も `LOCAL_API_HOSTS` / `LOCAL_API_PORTS` を
  参照する実際の実装に合わせた
- 持ち出しアーカイブの上限の説明から「爆弾ファイル」「展開すると巨大化」を削除。
  `portable_history.py` に圧縮・展開の実装はないため、decompression bomb に相当する
  仕組みではない。過大な入力・長い文字列・多数のレコード・深い入れ子によるメモリ消費と
  処理負荷を制限する上限である旨へ変更
- 「DPAPI暗号は同じPCの同じユーザーでしか読めません」という絶対表現を、
  「DPAPI方式は通常、暗号化したWindowsユーザー環境に結び付くため、そのまま別PCへ
  持ち出す用途には向きません」へ変更
- `atomic_io.py` の説明から「書き込み中に落ちてもファイルを壊さない」「中断されない
  1つの操作」という保証表現を削除し、「既存ファイルが途中まで書き換わった状態になる
  リスクを避けるため、一時ファイルを完成させてから置換する」へ変更。用語集の
  「原子的 (atomic)」も「外部から途中状態が見えない単一操作として扱われる性質」へ変更
- 「6種類のスレッドが同時に動いています」を訂正。常駐するもの、音声入力ON時のみ
  起動するもの、処理のたびに生成されるものが混在するため、「複数の用途別スレッドを
  必要に応じて使う」へ変更し、表に存在するタイミングの列を追加
- 「できること」の表で健康記録・家計簿の登録を「有効」と記載していた点を
  「利用可能（モード選択時）」へ変更。`Controller._active_mode` の既定は `"default"` で、
  起動しただけでは連携処理は動かない
- ストリーミングの「1文字ずつ受け取り」を「生成された文字列断片が届くたび」へ訂正。
  実装は `choice["delta"]["content"]` の文字列を受け取っており、1回が1文字とは限らない。
  「句点「。」が来るたび」も、文境界が `_BOUNDARIES = frozenset("。！？!?\n")` の
  5種類である旨へ訂正
- GGUFの破損検出について「黙って誤動作することはありません」という保証を削除。
  読み込み時に例外になる場合はあるが、あらゆる破損・取り違え・差し替え・改変を
  確実に検出できる保証はない（アプリ側でモデル本体の期待SHA-256を検証していない）
- Whisperの誤生成について「YouTubeの字幕データで学習した名残」という原因の断定を削除。
  本プロジェクトのコードから確認できないため。100文字超の破棄も「一度の発話でそこまで
  長くならない」という一般化をやめ、誤認識抑制のヒューリスティックである旨へ変更
- AES-GCMの説明にあった「利用者には、利用者には」の重複を修正
- 「外部へ送信」という表現を「連携APIへ送信」へ変更。送信先はlocalhostであり、
  インターネットへの送信と誤読されないようにした
- 総括の「自分のPCの中だけで完結する」を「主要な推論・音声認識・音声合成・会話処理を
  ローカルで実行する」へ変更（Whisperモデルの初回ダウンロードがあるため）
- 冒頭の「自分のパソコンの中だけで動く」も同様に変更。直後でWhisperモデルの初回
  ダウンロードをインターネット通信として説明しており、冒頭一文と矛盾していた
- 起動手順に残っていた「家計簿ブリッジを起動（ポート 8767）」を
  「（`KAKEIBO_BRIDGE_PORT`、既定 8767）」へ統一。同一文書内で固定値の断定が残っていた
- 「連携なしでも、チャット・音声入力・読み上げは問題なく動作します」を
  「連携機能を使用しなくても、チャット・音声入力・読み上げの各機能は連携APIを
  必要としません」へ変更。11章でGUI・推論・マイク・読み上げを実機未確認としており、
  動作保証と正面から矛盾していた
- スレッド表の「いずれも `daemon=True` で作られます」を訂正。表にメインスレッドを
  含めているが、Pythonのメインスレッドはデーモンスレッドではない。
  「メインスレッドを除き、本アプリが明示的に生成する上記のバックグラウンドスレッドは
  `daemon=True`」へ変更
- 「LLMは同時に2つ動かすとメモリ的にも計算的にも破綻します」という一般化を削除。
  コードから確認できるのは `_running` による同時実行の拒否であるため、
  「複数の処理が競合することやリソース使用量が増えることを避けるため、同時実行を
  許可していません」へ変更。「LLMとWhisperを同時に読み込むと落ちる」も
  「VRAM不足やロード失敗のリスクを抑えるため」へ変更
- 「テストコードは本体コードのおよそ半分の分量」「分量は本体コードの半分ほど」の2箇所を
  削除。行数と同じく更新のたびに変わる比率であり、冒頭方針に反していた
- ゲストモードの「ファイルは一切作られません」を「会話履歴ファイルを保存しません」へ
  変更。`_save_now` の早期リターンから言えるのは会話履歴を保存しないことに限られる
- 7章の「同じWindowsユーザーなら復号できる方式」を「原則として暗号化時と同じWindows
  ユーザー環境で復号する方式」へ変更。6章⑧で例外を説明した内容と一貫させた
- 総括の「5層のチェックを通さないと外部へ送らない」を「連携APIへ送らない」へ統一。
  6章⑩の見出しも「外部連携の多層防御」から「連携API送信の多層防御」へ変更
- 用語集の「VRAM: 足りないとアプリが落ちる」を「不足するとモデルの読み込みや推論が
  失敗することがある」へ変更
- 履歴一覧キャッシュの「内容ハッシュで確実に検出しています」を「検出できます」へ、
  プリロールの「地味に重要」を「役割」へ変更
- 停止処理の説明から「メモリ破壊」を削除。コードとコメントに裏付けがないため、
  「状態競合や予期しない動作、クラッシュにつながる可能性がある」へ変更
- 要約の `temperature=0.3` の説明から「創作しないで正確に」を削除。temperatureを
  下げることが正確性を保証するわけではないため、「通常生成（既定0.7）より低く、
  出力のばらつきを抑えている」へ変更
- scryptの説明から「総当たり攻撃で秒間何百万回も試されます」を削除。試行速度は方式・
  ハードウェア・実装・パラメータに依存するため、「パスフレーズから単純に鍵を作る方式では
  高速な総当たり攻撃を受けやすい。scryptは計算時間とメモリを必要とする鍵導出関数で、
  総当たりの試行コストを高くするために使う」へ変更（`N=32768, r=8, p=1` は実装値のため維持）
- `InvalidTag` の説明から「攻撃者に手がかりを与えないためのセオリー」を削除。設計意図を
  裏付けるコメントや仕様記載がないため、「`InvalidTag` だけでは原因を区別できないため、
  本実装では同じエラーとして表示する」という事実の記述へ変更
- 用語集の「Tkinter: Python標準のGUI部品集。追加インストール不要」から可用性の断定を削除し、
  「Python標準のGUIインターフェース」へ変更。標準ライブラリに含まれることと、
  すべてのPython環境で必ず利用できることは同義ではない
- 「`pynvml`（NVIDIAの公式ライブラリ）」を「NVMLのPythonバインディング（import名
  `pynvml`、`requirements.txt` では `nvidia-ml-py`）」へ変更。NVML本体とPython
  バインディングの混同を解消
- 根拠のない比率表現を削除。「安全側に倒れるかを確かめるテストが大半です」を
  「安全側の挙動を確認するテストが多く含まれています」へ、「ここからはほぼすべてが
  セキュリティと堅牢性の変更です」を「この時期はセキュリティと堅牢性に関する変更が
  増えています」へ変更（件数を集計していないため）

### 変更
- 評価的・断定的な表現を事実ベースへ変更。「力作」「執念が見える」「うまい点」
  「珍しい水準」などの自己評価的な表現を、動作と目的の説明へ統一
- 作者固有の実行環境に依存する製品名を一般化（「開放型イヤホン環境」）
- `公開リポジトリ_SHA256一覧.md` をv1.6.3の最終状態へ更新

### 備考
- **ロジック変更はない。** `LLM_Local_Chat.py` の変更は `APP_VERSION` の1行のみ
- 回帰テストは実行していない。コードロジックを変更していないため、v1.6.1時点の
  402件全通過が最後の確認結果

---
## [1.6.2] - 2026-08-18

### 修正
- 公開ドキュメント `コード解説_やさしい版.md` の記述を修正。利用者の判断に影響する
  次の誤りと不足があった
  - GGUFモデルが同梱されていると読める記述。実際はリポジトリに含まれず、各自での
    ダウンロードと配置が必要
  - マイク入力と読み上げが既定で有効であるかのような記述。実際は `mic_enabled` と
    `tts_enabled` のいずれも既定 `false`
  - 家計簿・健康記録への登録が本アプリ単体で動くかのような記述。実際は別途ローカル
    APIが必要で、家計簿側は2026-08-18時点で非公開
  - 会話履歴の暗号化を無条件の安全性として記載していた点。DPAPIは同一Windowsユーザー
    の権限で動くプログラムからは復号可能であり、設定ファイルとテキスト書き出しは
    平文であることを明記
- Pythonコードの規模を「約10,300行」と記載していた誤りを修正（実測は本体とモジュール
  群の合計で7,165行）
- 3列のヘッダーに2列の本文が続く不正なMarkdownテーブル2箇所を修正
- 送信値の検証範囲の表から筋肉量（`muscle_mass`、0〜200kg・両端を含まない）が
  欠落していた点を追記

### 変更
- `コード解説_やさしい版.md` から行数・ファイル件数・テスト件数の決め打ちを削除し、
  利用者が自分で数えるスクリプトへ置き換え。更新のたびに記述が古くなるため
- 罫線素片（`─` `│` `┌` `┼` など）と日本語を混在させた図・擬似表を、ASCIIのみの図と
  Markdownテーブルへ置き換え。罫線素片はUnicodeのAmbiguous幅で、日本語フォントでは
  2列・欧文フォントでは1列に描かれるため桁が揃わなかった
- SHA-256一覧を `公開リポジトリ_SHA256一覧_20260817.md` から
  `公開リポジトリ_SHA256一覧.md` へリネームし、v1.6.2の最終状態へ更新。
  この一覧はリポジトリの現行状態に追随して随時更新するため、ファイル名に日付を持つと
  ファイル名の日付と中身の時点がずれる。**ファイル名は固定し、時点と対象バージョンは
  文書の冒頭で管理する**方針へ変更した

### 備考
- **コードロジックの変更はない。** 公開ドキュメントの記述と版番号のみの修正
- v1.6.1のCHANGELOGへ記載が漏れていた変更を、本版で記録する。公開済みのv1.6.1
  エントリは書き換えない
  - `SYSTEM_PROMPT` に「プライバシーに配慮し、ユーザーの個人情報や機密情報を推測して
    返答しないでください」の指示を追加。コード自体はv1.6.1時点で反映済みで、
    変更履歴への記載のみが漏れていた
- 回帰テストは実行していない。コードを変更していないため、v1.6.1時点の402件全通過が
  最後の確認結果

---
## [1.6.1] - 2026-08-17

### 修正
- 家計簿モードで、原文に年付きの日付（`2026/08/16`・`2026-08-16`）が明示されていても、
  登録確認画面の日付が実行日へ置き換わる不具合を修正
- 年付き日付は原文の年をそのまま採用し、実行年での補完は年を含まない月/日形式
  （`7/17`）に限定するよう `kakeibo_date.py` の日付抽出を変更

### テスト
- `tests/test_kakeibo_date.py` に年付き日付の回帰テストを追加（11件）。合計402件

### 備考
- 年付き日付形式はv1.6.0以前も未対応で、`YYYY/MM/DD` と `YYYY-MM-DD` はいずれも
  実行日へフォールバックしていた。過去の修正が失われた回帰ではなく未対応形式の追加
- 区切り文字の混在（`2026/08-16`）と `2026年8月16日` 形式は引き続き未対応
- 金額・カテゴリ・店舗の解析仕様、ポート設定は変更していない

---
## [1.6.0] - 2026-08-17

### 変更
- 家計簿ブリッジへの既定接続先を `http://localhost:8765` から `http://127.0.0.1:8767` へ変更
  （`8765` は同一端末上の別サービスが使用しているため、家計簿ブリッジ専用に `8767` を割り当て）
- ローカルAPI送信の許可ポート `LOCAL_API_PORTS` を、固定値ではなく
  `KAKEIBO_BRIDGE_PORT` と連動する形へ変更

### 追加
- 環境変数 `KAKEIBO_BRIDGE_PORT` で家計簿ブリッジのポートを上書きできるよう追加
  （1〜65535 の範囲外は起動時にエラー）。ポート競合時の自動振替は行わない

### 文書
- `README.md` のポート記述を `8767` へ更新

### テスト
- `tests/test_integrations.py` の許可ポート判定を `8767` 基準へ更新（6件）

### 備考
- 家計簿API（`8000`）、Biolog API（`8766`）、Biolog UI（`8501`）のポートは変更していない

---
## [1.5.2] - 2026-07-28

### 追加
- Python 3.12・Windows 64bitのCPU専用環境向けに、CPU版PyTorchと`llama-cpp-python`を使用する`requirements-cpu.txt`を追加
- READMEの依存導入手順をCUDA 12.4向けとCPU専用向けに分離

### UI
- 暗号化エクスポートを「現在の保存済み会話」と「保存済みの全会話」の明示的なメニューへ分割
- エクスポート先・パスフレーズ・確認入力を1画面にまとめ、対象範囲と未保存部分が含まれないことを表示
- インポートするファイルとパスフレーズを1画面にまとめ、重複処理を「スキップ」「コピー」「キャンセル」の明示的なボタンへ変更

### 修正
- 現在会話のエクスポート時に、実行時のパスを固定し、暗号文を読む直前にも管理対象パスか再検証
- インポート前の重複件数に、アーカイブ内で重複する会話も含めるよう修正
- 非同期処理開始前に操作中フラグを設定し、スレッド開始失敗時にも確実に解除
- 無効・過大なインポートファイルをTk例外へ漏らさず、利用者向けエラーとして表示
- 重複確認画面の生成・表示失敗時も操作中フラグを解除し、不正な選択値をキャンセル扱いに変更
- エクスポート先を`.shiro-export`へ限定し、確認後に出力先が作成・削除・変更された場合は書込みを中止
- 重複確認画面に、スキップ時とコピー時の現時点の追加予定件数を表示

### テスト・文書
- ゲスト・未保存会話の拒否、ディスク再読込、全会話へのメモリ内容非混入、管理外パス、重複方針、操作中フラグ、無効入力、出力先競合の回帰テストを追加
- v1.5.1で回帰テスト3件を追加したとする不正確な記載を訂正

---

## [1.5.1] - 2026-07-27

### 修正
- エクスポート実行で、ゲスト会話や未保存会話が出力できてしまう不具合を修正

### 訂正
- 当初記載していた「回帰テスト3件の追加」は実際のv1.5.1差分に含まれていなかったため、v1.5.2で追加

## [1.5.0] - 2026-07-27

### 追加
- 別PC・別Windowsユーザーへ会話履歴を移すための`.shiro-export`形式を追加
- scrypt固定設定とAES-256-GCMのAADにより、履歴本文・追加フィールド・暗号設定を認証付き暗号化
- インポート前の全件検証、重複判定、保存期間警告、DPAPI再暗号化、通常例外時の巻き戻しを追加
- ヘルプへ基本操作、音声、連携、暗号化移行、復旧の説明を追加

### 変更
- 平文の「テキストとして保存」を廃止し、パスフレーズ付き暗号化エクスポートへ置換
- バージョンを1.5.0へ更新
- `cryptography 49.0.0`を固定依存として追加

### セキュリティ
- パスフレーズ、復号済み履歴、暗号鍵を設定・ログ・平文一時ファイルへ保存しない
- ポータブル形式のサイズ、件数、入れ子、文字数、KDF・nonce・saltを厳格検証
- 誤パスフレーズと暗号文改ざんを同じ利用者向けエラーとして処理

---

## [1.4.3] - 2026-07-27

### セキュリティ
- ローカルAPI URLのuserinfoを拒否し、HTTPリダイレクトを追跡しないよう変更
- ローカルAPI応答の読み取りサイズを1MiBに制限
- 会話履歴の各発言要素と本文フィールドの型検証を追加
- READMEから非公式`pipwin`導入手順とユーザー全体のPowerShell実行ポリシー変更手順を削除
- 非推奨の`pynvml`配布パッケージを、同じ`pynvml`モジュールを提供する`nvidia-ml-py`固定版へ変更

### 修正
- 暗号化履歴が同一サイズ・同一タイムスタンプで原子的置換された場合も、内容SHA-256で一覧キャッシュの変更を検出

### UI
- 「ヘルプ」メニューに基本操作、履歴暗号化・復旧、バージョン情報を追加

### 公開整備
- MIT Licenseへ変更し、READMEの独自禁止条件を削除
- 作者PC専用のロールバック文書と旧操作マニュアルを公開対象から削除
- バージョンを1.4.3へ更新

---

## [1.4.2] - 2026-07-27

### セキュリティ
- 会話履歴の本文・タイトル・要約を現在のWindowsユーザー用DPAPIで必須暗号化し、平文フォールバックを廃止
- v1.4.1以前の平文履歴を、元バイト列の復号照合と原子的置換を行って初回起動時に移行
- 暗号化・復号・移行に失敗した場合は通常起動または保存をfail-closedとし、UIで通知
- 明示的なテキスト保存前に、暗号化されない平文であることを毎回警告

### 追加
- 会話履歴の保存期間を無期限・30日・90日・180日・365日から選択できる設定を追加。既定は無期限
- 会話の最終活動日時をUTCで暗号化本文内に保持し、期限超過候補の確認後に削除
- READMEの導入・運営責任者向け説明に、DPAPIの限界、Windowsアカウント分離、BitLocker、ACL、バックアップ保持を追加
- DPAPI利用不可、復号不能、移行ロック残留時に、安全な復旧手順をUIとREADMEで案内
- 移行開始前と完了前に履歴を再検査し、残存する平文・異常履歴がある場合は通常起動を中止

### 修正
- `pywin32`欠落などでDPAPIを初期化できない場合に、コンソール出力だけで終了せずエラーダイアログを表示
- 履歴安全確認をAvatar/TTSワーカー作成前に行い、起動失敗時はResourceMonitorを停止
- 残留した`.history_migration.lock`を自動解除せず、全Shiro終了確認後の手動復旧を案内
- 履歴移行直前に元バイト列を再照合し、同一サイズ・同一更新日時に偽装された競合更新も上書きしないよう強化
- 移行失敗の診断情報をファイル名・処理段階・例外クラス名だけに限定して保持
- 履歴安全確認が完了するまでメインウィンドウを表示せず、空ウィンドウが残る問題を修正
- 履歴一覧キャッシュの署名に作成・変更時刻を加え、同一サイズ・同一更新日時の原子的置換を検出
- ロールバック文書を2段階バックアップと新規4ファイルの構成に訂正

### 互換性
- 履歴保存形式に後方互換性のない変更があります。暗号化後の履歴はv1.4.1以前では読み込めません
- DPAPI暗号文はWindowsユーザープロファイルと環境に依存し、環境変更後の復号を保証しません

---

## [1.4.1] - 2026-07-27

### ドキュメント
- READMEにv1.4系の主要改善、生成設定既定値、ローカルAPIの公開範囲を追記
- GitHub公開対象から内部用仕様書・計画書・プロンプト履歴を除外

---

## [1.4.0] - 2026-07-27

### 追加
- 設定画面にWhisper実行モード（自動・GPU small・GPU medium・CPU small）を追加し、選択モデルとデバイスを診断ログ・ステータスへ表示
- 通常生成と要約を同一`LLMService`で直列化し、操作世代IDで古いコールバックを無効化する排他制御を追加
- チャット履歴検索用にタイトル・要約のメモリインデックスを追加し、変更されたJSONだけを再読込する回帰テストを追加
- ガード・エラー・要約・停止・VAD設定を対象とする`unittest`回帰テストを追加
- LLM応答のストリーミング表示中に、文末(`。`・`！`・`？`・改行)まで生成された文から順次TTSで読み上げる機能を追加
- 生成設定にSAPI5読み上げ速度(`tts_rate`, `-10`〜`10`, 既定値`0`)を追加。保存後、次の読み上げから再起動なしで反映
- ストリーミングTTSの文分割、コード/JSONブロック除外、TTS/Whisper競合、速度検証、SAPI完了待機を確認する回帰テストを追加
- Whisper認識ごとに入力デバイス、録音時間、RMS、使用モデル、セグメント品質指標(`no_speech_prob`・`avg_logprob`・`compression_ratio`)、送信/破棄理由を出力する診断ログを追加(音声データは保存しない)

### 変更
- 設定・履歴・アバターと相対モデルパスを、起動時のカレントディレクトリではなくアプリ配置フォルダ基準で解決するよう変更
- 生成速度ログの`tokens_per_sec`をストリームチャンク数ではなく、モデルtokenizerで数えた実出力トークン数から算出するよう変更
- モデル再読込時に旧モデルを明示解放し、世代IDによる古い結果の破棄と失敗時の旧設定復旧を追加。終了時はLLM・音声・TTS・API処理を最大6秒監視
- 自動モードではLLMロード後の空きVRAMが4096MB以上ならWhisper medium、2048MB以上ならsmallをGPUへ配置し、それ未満ではCPU smallへフォールバックするよう変更
- VRAM安全判定を固定7,000MBからGPU総容量・実空き容量・動的予約領域ベースへ変更し、LLMをWhisperより先にGPUロードするよう起動順を直列化
- 8GB級GPUではLLMを優先し、空き容量が4,096MB未満の場合はWhisper mediumをロードせずCPU smallへフォールバックするよう変更
- 起動時にCUDAオフロード対応、要求GPUレイヤー、LLMロード前後のVRAM内訳、Whisper配置方針を診断ログへ表示
- TTSのみ有効な構成ではWhisperをロードせず、起動発話をWhisper準備処理から独立して一度だけ実行するよう変更
- トークンコストキャッシュを最大2048件のLRUへ変更
- TTS実装に合わせて未使用の`pyttsx3`依存を削除し、CPU/CUDA別のPyTorch導入手順を整理
- 複数文の連続読み上げを1つのTTSバッチとして管理し、文間でアバターとマイク抑制状態が解除されないよう変更
- SAPI5の`SpVoice`を文ごとの生成からTTSワーカースレッド内での再利用へ変更
- 通常再生から`PurgeBeforeSpeak`を除外し、`RunningState`監視を`WaitUntilDone(50)`による完了待機へ変更。Purgeは停止要求時のみ実行

### 修正
- 実機で観測された高圧縮・中程度no-speechのWhisper無音誤認識（`compression_ratio >= 2.40`かつ`no_speech_prob >= 0.50`）を、正常発話の保護帯を維持しながら送信前に破棄
- 停止要求から3秒以内にLLMワーカーが終了しない場合、UIロックを偽装解除せず再起動を案内し、操作世代を確認しながら遅延終了を監視するよう修正
- TTS終了後800ms以内に次の発話が始まるとマイク抑制が誤解除される競合を、既存のTTS世代カウンタで防止
- マイク切断・ドライバ障害時の読取エラーを、秘密情報を含めず30秒間隔で記録し、復旧も確認できるよう修正
- Biolog送信前に日付・型・有限値・API準拠の数値範囲を検証し、NaN/Infinityやネストした非文字列値を含むレコードを全体拒否するよう修正
- 複数の健康JSONがある応答では厳格JSONとして有効な最後の候補を採用し、先行する例示JSONを誤採用する問題を緩和
- LLMの`reset()`失敗時に性能計測が未初期化の`reply`を参照する問題を修正
- 未参照だったストリーミング位置属性`_stream_mark`の代入を削除
- 健康測定値だけの入力をLLMが`memo`へ複製し、既存メモを上書きする問題をプロンプト制約と送信前サニタイズで防止
- 健康JSONが構文的に正しくても全項目が空の場合は、送信を諦めずJSON専用再抽出を1回行うよう修正
- 健康記録で`memo: null`を送信せず、行動・食事・メモだけの記録をBiologへ連携できるよう修正
- 健康モードで明示されたメモ・備考をBiologの`memo`へ登録できるよう修正
- 健康記録JSONを応答先頭で確定して表示・TTS・履歴から除外し、途中切れ時は排他制御下でJSON専用再抽出を1回行うよう修正
- 健康測定値が行動ログへ重複登録される問題を、プロンプト制約とサニタイズで防止
- 健康記録・家計簿モードの履歴とタイトルに、LLM向け指示文ではなくユーザー原文だけを保存するよう修正
- 要約中・停止待機中・履歴切替後に同一`Llama`へ並行アクセスできる経路を修正
- VRAMガード遮断と生成エラーを会話履歴・要約・TTSへ混入させず、入力原文を再送可能にするよう修正
- VAD閾値を1〜32767に検証し、boolや既存の不正設定値を既定値へ戻すよう修正
- Whisperセグメントの品質指標を使い、低信頼無音(`no_speech_prob >= 0.60`かつ`avg_logprob <= -0.90`)と高圧縮反復(`compression_ratio >= 2.40`)を送信前に破棄する保守的なハルシネーションフィルターを追加
- Whisper解析中にTTSが開始された場合、世代カウンタで一時的な重複も検出し、認識結果を送信前に破棄するよう修正(自己音声による再送信ループを防止)
- 高い読み上げ速度や短い冒頭文で、非同期SAPI再生の開始競合により文頭または文全体が欠ける可能性を修正
- 停止処理(`Controller.stop`)の `_voice.tts_active` を実フラグ名 `_tts_active` に修正(旧コードは未使用属性を作るだけで、停止時の即時マイク排他解除が効かなかった)
- 起動時にWhisperロードをスキップした場合、マイクボタン押下時に「設定で起動時マイクを有効にして再起動」の正しい案内を表示(従来は「読み込んでいます」と表示され続けた)

### リポジトリ整備
- `gitignore` をドット付き `.gitignore` にリネームして除外を有効化(`backup_before_*/`・`OLD_DATA/`・`*.bak`・`*.bak_*`・`advanced_settings.json` を追加)
- v1.2.0〜v1.3.0のモジュール分離11ファイルと速度最適化を含む現行実装をコミット固定
- 現行仕様書 `docs/CURRENT_SPEC.md` を追加(コード読解に基づく現行仕様の基準資料)

---

## [1.3.0] - 2026-06-25

### 追加
- 段階的リファクタリングにより、`LLM_Local_Chat.py` から主要責務を分離
  - `controller.py`: 送信・停止・LLM完了処理のオーケストレーション
  - `prompt_builder.py`: 通常・家計簿・健康記録プロンプト生成
  - `json_extractors.py`: 家計簿/Biolog向けLLM応答JSON抽出
  - `session_store.py`: `chat_logs/` の保存・読み込み・一覧・検索・削除・名前変更
  - `audio_workers.py`: `TTSWorker` / `VoiceRecognizer`
  - `app_composition.py`: 起動時依存生成 (`AppDeps`, `create_app_deps`)
  - `integrations.py`: 家計簿/Biolog送信前ガード・サニタイズ・確認ダイアログ・POST処理
- Biolog/家計簿送信前の安全ガードを追加
  - サニタイズ済みpayloadのみ確認ダイアログに表示
  - `localhost` / `127.0.0.1` / `::1` 以外のAPI URLを拒否
  - `https://localhost`、`localhost.evil.com`、malformed URL、port不正URLを拒否
  - LLM出力の未知キー、`user_id`, `token`, `url`, `headers` を破棄

### 変更
- `Controller` の `sys.modules["__main__"]` 依存を廃止し、`ControllerDeps` 経由の明示依存注入へ変更
- `PromptBuilder` に家計簿・健康記録プロンプト生成を移動し、`ChatApp` のUI責務を軽量化
- import時の `ResourceMonitor` / `VRAMGuard` / `WhisperPool` 生成を廃止し、`main()` 起動時の `create_app_deps()` で生成する構成へ変更
- `ChatApp` は `AppDeps` を受け取り、`SessionStore`、`WhisperPool`、`ResourceMonitor` を明示的に利用する構成へ変更
- `TTSWorker` / `VoiceRecognizer` は `audio_workers.py` へ移動し、音声スレッド・VAD・TTSの既存挙動を維持
- 家計簿/Biolog連携は `IntegrationBridge` に分離し、`ChatApp` にはController互換の薄い委譲メソッドのみ残す構成へ変更

### 修正
- 家計簿の `amount` 判定で `bool` を明示的に拒否
- Biologの `user_id` はLLM出力を採用せず、送信時に固定値 `self` を付与する挙動を明確化
- `LLM_Local_Chat.py` を import しただけでTkウィンドウ生成や重い依存生成が走らない構造に修正

### 検証
- `py_compile` による構文確認
- `import LLM_Local_Chat` / `import app_composition` / `import integrations` の副作用確認
- JSON抽出、プロンプト生成、SessionStore、IntegrationBridge、audio_workers の軽量直接確認
- Dドライブ実行用フォルダへの明示ファイルコピーと、D側 `.venv` での import / py_compile 確認

---

## [1.2.0] - 2026-05-06

### 追加
- **VRAM安全フィルタ** (`resource_monitor.py` 新規作成)
  - `ResourceMonitor`: 0.5秒周期で VRAM/GPU/CPU を収集するデーモン（pynvml → nvidia-smi → CPU-only でフォールバック）
  - `VRAMGuard`: `vram_score = vram_ratio + gpu_pct * 0.001` による即時安全判定（予約・状態なし）
  - `adjust_llm()`: ロード前の `n_gpu_layers` 自動調整（VRAM使用率 > 85% → CPU専用）
  - `adjust_inference()`: 推論直前の `max_tokens` 段階削減（レンジ化バッファ +500/800/1200MB）+ Whisper ΔWhisperGPU% スパイク検知
  - `WhisperController`: GPU/CPU ヒステリシス切替（88%→CPU、70%→GPU）+ `delta_gpu_pct` 変化量追跡
  - `WhisperPool`: GPU版(medium) + CPU版(small) を起動時に両ロード、ゼロコスト切替

### 変更
- `init_llm()`: `adjust_llm()` による `n_gpu_layers` 自動調整（VRAM高負荷時 CPU 実行へフォールバック）
- `_load_whisper_async()`: 従来のデバイスループ → `WhisperPool.load()` に置き換え（GPU+CPU 同時ロード）
- `VoiceRecognizer._loop()`: `self.whisper_model.transcribe()` → `self.whisper_model.get_model()` + `model.transcribe()` に変更（ヒステリシス付き切替）
- `_llm_worker()`: `adjust_inference()` による `max_tokens` 動的調整を追加。VRAM 危機時は推論中止しメッセージを表示
- `chat_settings.json`: `vram_danger_gpu_pct`, `vram_danger_vram_pct` フィールドを追加

### 設計思想
- 「VRAMを管理する」ではなく「VRAMから逃げる」設計
- OOM完全防止は不可能。「クラッシュではなく劣化」で済ませることが目標
- VRAM が唯一の判断軸（GPU% は参考ログのみ。WhisperController のみ GPU% を使用）
- 既知の限界: TOCTOU あり・単一プロセス内のみ有効・確率的削減が目的

---

## [1.1.2] - 2026-05-06

### 修正
- `_strip_code_blocks`: `re.DOTALL` 依存を廃止し `[\s\S]*?` パターンに変更  
  → 言語指定あり・なし・複数行すべてのコードブロックを確実に TTS から除去
- `_health_build_prompt`: `meal_detail` のハルシネーション抑制  
  → フィールド説明に「ユーザーの発言通りの表記を使用、言い換え・造語・変換禁止」を明示

### 変更
- `activity_log` フィールド説明を「運動のみ」→「運動・作業・出来事など今日の活動内容」に拡張  
  → 「アプリのアップデートをした」などの日常活動報告も記録対象に

---

## [1.1.1] - 2026-05-06

### 修正
- TTS読み上げからコードブロック（` ``` ... ``` `）を除外 — チャット画面への表示は変更なし
- `import re` をトップレベルに一本化（`_extract_kakeibo_json` / `_extract_health_json` 内の重複を削除）

### 追加
- `_strip_code_blocks(text: str) -> str` ユーティリティ関数（モジュールレベル）

---

## [1.1.0] - 2026-05-06

### 追加
- **Biolog連携**: 健康記録モード（💪ボタン）でLLMレスポンスから体重・体脂肪等をJSON抽出し、Biolog API (`localhost:8766/api/health/record`) へ自動POST
- `BIOLOG_URL` 環境変数によるエンドポイント切り替え対応（デフォルト: `http://localhost:8766`）
- `request_id` (UUID v4) による冪等性キー付与（skills.md §3-2 準拠）
- `meal_detail` + `activity_log` → `memo` フィールドへの自動変換
- HTTPError (422等) の詳細エラーメッセージをチャットに表示
- `import uuid` 追加

### 修正
- `HEALTH_API_URL` が未接続ポート(`8001`)を指していた問題を修正 → `BIOLOG_API_URL` (`8766`) に変更
- `_health_build_prompt` の `muscle_mass` 単位を骨格筋率(%) → 筋肉量(kg) に修正（Biolog スキーマ準拠）
- `protein_intake` フィールドをLLMプロンプトから削除（Biolog スキーマ外フィールド）

### 変更
- `_send_to_health_api` → `_send_to_biolog_api` に刷新（Biolog スキーマ対応）
- `_on_llm_done` 内の健康記録処理を `tts.speak()` 後に移動（API送信とTTSを並行実行）
- README.md に v1.1.0 バッジ・Biolog連携の使い方・環境変数説明を追記
- CHANGELOG.md を新規作成 → 既存エントリへの追記に変更

### アーキテクチャ（維持）
- `LLM_Local_Chat.py`: Windowsホスト実行を維持（SAPI5 TTS は Windows COM 専用のため Docker 非対応）
- `biolog-api`: Dockerコンテナ (docker-compose.yml, port 8766) — 変更なし
- `_tts_active` フラグ（VoiceRecognizer の VAD 排他制御）: 変更なし

---

## [1.0.6] - 2026-05-04

### 修正
- 停止ボタンを押した後にチャットを入力するとアプリが強制終了する問題を修正
    - `_stop_all`の冒頭で即座に`_is_thinking=True`・`_btn_send=DISABLED`を設定し、LLMリセット完了まで次の送信をブロックするよう変更
    - LLMリセット完了後に`_unlock`でUIを解除する方式に変更し、リセット中の二重送信によるクラッシュを防止
    - TTS起動発話ロジックの適正化
        -TTSWorker 内での直接発話を廃止し、メインアプリ（ChatApp）側の初期化完了タイミングで発話するように変更。これにより、バックグラウンドでの予期せぬブロッキングを防止。
    - LLM停止処理の安全性向上
        - ストリーミング生成中に llm.reset() を呼び出すと発生していた C++ レベルのクラッシュ問題を修正。
        - 停止ボタン押下時、即座にリセットせず、ワーカーのスレッド終了を安全に待機してからUIを解放するフロー（_wait_and_unlock）を導入。
    - フラグ管理の厳格化
        - 中断（Abort）時のフラグ処理を見直し、中断後に不要な履歴保存やUI更新が重複して発生しないようガード処理を強化。
    - コードのクリーンアップ
        - 使用されなくなった古いリセット用メソッドや、不適切なコメントアウトの削除。

---

## [1.0.5] - 2026-05-03

### 修正
- 停止ボタンが効かない不具合を修正
- TTSがOFFでも発話する不具合を修正
- 生成設定でマイク・TTSが両方ONになって起動する不具合を修正
- コードの大幅修正によりアプリが起動しなかった不具合を修正
- マイクにチェックが入らない不具合を修正
- 設定ダイアログ変更時に起動発話が誤って発火する不具合を修正
    - 起動発話は`_initial_greeting_done`フラグで管理し、アプリ起動時の1回のみ実行するよう変更

### 変更
- `requirements.txt`に`pywin32==311`を追加
- TTSエンジンをPowerShell（SAPI5）から`win32com`（pythoncom経由）に変更
    - `pywin32`パッケージを使用してWindows SAPIを直接呼び出すよう変更

### 既知の不具合（次バージョンで対応予定）
- `VoiceRecognizer`：Bluetoothデバイス等の遅延でクラッシュする場合がある
- `ChatApp`：`_on_tts_stop`の0.8秒待機がスレッドを乱発する

---

## [1.0.4] - 2026-05-03

### 修正
- TTSWorker（読み上げ機能）の安定性向上
    - `_run`メソッド内の二重ループ構造を解消し、処理を一本化することで動作の不安定さを解消
    - PowerShell（SAPI5）呼び出し時のテキストエスケープ処理とプロセス終了処理を最適化
- 音声認識（VoiceRecognizer）のデバイス判定を修正
    - Whisperモデルの`device`属性を直接参照するように変更し、CPU/GPUの判定精度を改善

### 変更
- `import torch`を追加
- AvatarWindowクラスのリファクタリング
    - 画像読み込み・リサイズ処理（`_load`メソッド）を統合し、コードの重複を排除
    - アバターの状態管理を整理し、初期化時の堅牢性を向上
- 全体的なコードのクリーンアップ
    - 各クラス間の重複ロジックを削除し、将来的な修正時のデッドロックや競合を抑制するための構造改善

---

## [1.0.3] - 2026-05-03

### 修正
- マイク・TTSを無効に設定した場合Whisperモデルを起動しないように修正
- VoiceRecognizerクラスの初期化エラー（AttributeError）を修正
    - 原因：`__init__`内で`self._enabled`が定義される前に`.clear()`が呼び出されていたため
    - 対応：属性の宣言順序を見直し、`threading.Event()`の初期化を先に行うよう変更

### 変更
- 動作環境のGPU推奨VRAMを6GB以上→8GB以上に変更
- 生成設定の「温度」を「会話の自由度」へ変更
- 生成設定に各項目の説明文を追加

---

## [1.0.2] - 2026-05-02

### 修正
- 初期起動時にマイク・TTSがOFFになるように修正
- 起動時の待機時間を10秒に設定（TTS着火安定化）
- `_stop`フラグの判定とクリアの順序を修正（2回目以降のTTSが再生されない問題）
- 終了時にmic_enabled・tts_enabledを設定ファイルに保存するよう修正
- メニューのTTS切り替え時に即座に設定ファイルへ保存するよう修正（`_toggle_tts`メソッド追加）
- `_open_settings`にmic_enabled・tts_enabledの初期値渡しと反映処理を追加
- TTS有効時に起動発話「システムを起動しました。」を追加（PowerShell初期化兼用）

### 変更
- 実行ファイル名を`tk_chat_local.py` → `LLM_Local_Chat.py`に変更
- 起動バッチファイル名を`AIローカル対話型AI.bat` → `LLMローカル対話型AI.bat`に変更
- README：GitインストールのTrueTypeフォント文字化け注意事項を追加
- README：「必ずお読みください」セクションを追加（音声ハルシネーション対処法）
- README：動作確認済みモデル一覧にgemma-4-E4B-Q4_K_M・LFM2.5-1.2B-Instruct-Q4_K_Mを追加

---

## [1.0.1] - 2026-05-01

### 修正
- アバターパスのスラッシュ抜けを修正
- ローカルパスを相対パスに変更（ポータビリティ向上）
- マイクボタンの動作説明を修正（起動後自動ON・ボタンでOFF）
- 停止ボタンの説明を修正（タイミングによっては中断できない場合がある旨を追記）
- phi-4の動作確認済み表記をphi-3.5に修正
- マイクON設定で起動した際にマイクが機能しないバグを修正
- VoiceRecognizer初期状態をOFFに変更・設定ファイルの値を優先して反映するよう修正
- マイクのデフォルトをONからOFFに変更
- TTSのデフォルトをONからOFFに変更

### 追加
- 設定ダイアログに「起動時にマイクを有効にする」「起動時にTTSを有効にする」チェックボックスを追加
- マイク・TTSの起動時状態を設定ファイル（chat_settings.json）で永続化するよう修正
- README：仮想環境の作成手順を追加
- README：Python・Git・CUDA Toolkitのインストール案内を追加
- README：Whisper初回ダウンロード（約1.5GB）の注意を追加
- README：Privateリポジトリでのclone方法の説明を追加
- README：動作確認済みモデル一覧を追加
- README：モデルリンク切れ時の案内を追加
- README：モデル利用規約に関する免責事項を追加
- README：「導入責任者の方へ」セクションを追加（セキュリティ特性・推奨事項）
- requirements.txtにsetuptools==78.1.1を追加
- chat_settings.json.exampleを追加
- .gitignoreを追加
- CHANGELOG.mdを追加
- 操作マニュアル（PDF）を追加

### 変更
- 動作保証をWindows 11のみに変更（Windows 10を除外）
- README：pip installコマンドに--no-cache-dirを追加
- README：ゲストモードの説明を正確な動作に修正
- README：「作者に直接お問い合わせ」をPAT発行案内に変更
- README：音声入力・TTSの説明をデフォルトOFFに合わせて修正
- README：セキュリティ項目にモデルによる外部通信の可能性がある旨の注意を追加
- README：diskcache（CVE-2025-69872）の脆弱性情報とpip-auditでの定期確認を推奨する旨を追加
- 免責事項を強化（禁止事項・保証の否認・準拠法を追加）
- ライセンス表記をMITからAll Rights Reservedに変更

---

## [1.0.0] - 2026-04-30

### 初回リリース

#### 主な機能
- ローカルLLM（llama-cpp-python）によるストリーミングチャット
- PyAudio + RMS-VAD + Whisper（medium）による常駐音声認識
- Windows SAPI（PowerShell経由）によるTTS読み上げ
- アバターウィンドウ（瞬き・口パクアニメーション）
- 会話の要約メモリ（長期会話対応）
- チャット履歴の保存・読み込み・検索
- ゲストモード（会話を保存しないプライベートモード）
- ダークテーマUI
- 生成設定（モデルパス・コンテキスト長・温度・VAD感度）

#### 動作確認済みモデル
- gemma-3-4b-it-q4_k_m.gguf
- phi-3.5-Q8_0.gguf

---

*以降のバージョンはこのファイルに追記していきます。*

plan modeでお願いします。
v1.1.0のBiolog連携は完璧に動作しました！ありがとうございます。
次のステップとして、使い勝手を向上させるために v1.1.1 の修正をお願いします。

【課題】
健康記録モード（💪）のON/OFFに関わらず、LLMが回答の中に ```json ... ``` ブロックを含めることがあります。
現在の実装では、TTS（読み上げ）がこのJSONコードブロックの中身まで読み上げてしまうため、非常に聞き取りづらいです。

【依頼内容：v1.1.1】
1. LLM_Local_Chat.py を修正し、tts.speak() に渡すテキストから ```（バッククォート3つ）で囲まれたコードブロックをすべて除外するフィルタ処理を追加してください。
2. 画面上のチャットログには JSON を表示させたままにし、あくまで「読み上げ（音声）」からのみ除外するようにしてください。
3. CHANGELOG.md に v1.1.1 のエントリを追加してください。
4. 依存ライブラリ（requirements.txt）に変更が必要な場合は対応してください（標準ライブラリの re モジュールで十分かと思います）。

既存の _tts_active などの排他制御や、v1.1.0 で実装した Biolog 送信ロジックを破壊しないよう注意して実装をお願いします。